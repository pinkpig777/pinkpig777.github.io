/* This file is auto-generated by scripts/build-blog.mjs */

export type BlogPost = {
  slug: string;
  title: string;
  date: string;
  summary: string;
  tags: string[];
  html: string;
};

export const blogPosts: BlogPost[] = [
  {
    "slug": "042126_GPU_Cluster_Storage_Design",
    "title": "042126 GPU Cluster Storage Design",
    "date": "",
    "summary": "Storage Design for a Hybrid Login + Compute Node (Cluster Setup) Overview This document captures the reasoning, design decisions, and implementation details for setting up storage …",
    "tags": [],
    "html": "<h1>Storage Design for a Hybrid Login + Compute Node (Cluster Setup)</h1>\n<h2>Overview</h2>\n<p>This document captures the reasoning, design decisions, and implementation details for setting up storage on a GPU server that initially serves as both a <strong>login node</strong> and a <strong>compute node</strong>, with future expansion into a multi-node cluster.</p>\n<p>Instead of following a rigid \"textbook\" layout, we intentionally designed a system that balances:</p>\n<ul>\n<li>Performance</li>\n<li>Flexibility</li>\n<li>Operational simplicity</li>\n<li>Future scalability</li>\n</ul>\n<hr>\n<h1>Key Concepts</h1>\n<h2>LVM (Logical Volume Manager)</h2>\n<p>LVM provides a flexible abstraction layer over physical storage:</p>\n<pre><code class=\"hljs\">Physical Disk → PV → VG → LV\n</code></pre>\n<p>It allows:</p>\n<ul>\n<li>Pooling storage into a single volume group</li>\n<li>Dynamically allocating logical volumes</li>\n<li>Future expansion by adding disks</li>\n</ul>\n<p>However:</p>\n<blockquote>\n<p>LVM does not eliminate filesystem constraints (e.g., XFS cannot shrink)</p>\n</blockquote>\n<hr>\n<h2>ext4</h2>\n<p>A general-purpose filesystem focused on:</p>\n<ul>\n<li>Stability</li>\n<li>Simplicity</li>\n<li>Mature recovery tooling</li>\n</ul>\n<p>Best suited for:</p>\n<ul>\n<li>System partitions</li>\n<li>Conservative environments</li>\n</ul>\n<hr>\n<h2>XFS</h2>\n<p>A high-performance filesystem designed for:</p>\n<ul>\n<li>Large files</li>\n<li>High throughput</li>\n<li>Parallel I/O workloads</li>\n</ul>\n<p>Key characteristics:</p>\n<ul>\n<li>Excellent for data-intensive systems</li>\n<li>Widely used in production clusters</li>\n<li>Supports project quota (pquota)</li>\n</ul>\n<p>Limitations:</p>\n<ul>\n<li>Cannot shrink volumes</li>\n<li>Requires more deliberate planning</li>\n</ul>\n<hr>\n<h2>Project Quota (pquota)</h2>\n<p>XFS provides <strong>project-based quota</strong>, which allows limiting disk usage at the directory level.</p>\n<p>Unlike traditional user/group quota:</p>\n<ul>\n<li>Applies to directory trees</li>\n<li>Suitable for workload isolation (e.g., <code>/data</code> vs <code>/scratch</code>)</li>\n</ul>\n<hr>\n<h1>Problem Context</h1>\n<p>We are working with:</p>\n<ul>\n<li>~2TB local NVMe storage</li>\n<li>200GB already allocated for system (<code>/</code>)</li>\n<li>~1.8TB remaining</li>\n</ul>\n<p>Node role:</p>\n<ul>\n<li>Login node (interactive usage)</li>\n<li>Compute node (jobs, experiments)</li>\n</ul>\n<p>Future direction:</p>\n<ul>\n<li>Expand into multi-node cluster</li>\n<li>Add storage via SSD or NAS</li>\n<li>Avoid reliance on resizing/shrinking partitions</li>\n</ul>\n<hr>\n<h1>Design Options Considered</h1>\n<h2>Option 1: Hard Partition / LVM Split</h2>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate logical volumes for:</p>\n<ul>\n<li>Persistent data</li>\n<li>Temporary data</li>\n</ul>\n<h3>Pros</h3>\n<ul>\n<li>Strong isolation</li>\n<li>Easy observability (<code>df -h</code>)</li>\n<li>Clear mental model</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>Requires early capacity decisions</li>\n<li>XFS cannot shrink → misallocation is costly</li>\n<li>Less flexible during early-stage usage</li>\n</ul>\n<hr>\n<h2>Option 2: Directory-based Separation Only</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/scratch\n</code></pre>\n<h3>Pros</h3>\n<ul>\n<li>Simple</li>\n<li>No upfront partitioning decisions</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>No isolation</li>\n<li>High risk of disk exhaustion</li>\n<li>Not suitable for multi-user environments</li>\n</ul>\n<hr>\n<h2>Option 3: Single XFS + Project Quota (<strong>Chosen</strong>)</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/persistent\n/data/scratch\n</code></pre>\n<p>Use:</p>\n<ul>\n<li>XFS as the underlying filesystem</li>\n<li>Project quota (pquota) to enforce limits</li>\n</ul>\n<hr>\n<h1>Decision Trade-offs</h1>\n<p>This decision is fundamentally a trade-off between:</p>\n<h3>1. Isolation vs Flexibility</h3>\n<table>\n<thead>\n<tr>\n<th>Approach</th>\n<th>Isolation</th>\n<th>Flexibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Partition split</td>\n<td>Strong</td>\n<td>Low</td>\n</tr>\n<tr>\n<td>XFS + pquota</td>\n<td>Medium</td>\n<td>High</td>\n</tr>\n</tbody>\n</table>\n<p>We chose <strong>flexibility</strong> because:</p>\n<ul>\n<li>Early-stage workload is uncertain</li>\n<li>Data vs scratch usage ratio may evolve</li>\n<li>Repartitioning later is costly</li>\n</ul>\n<hr>\n<h3>2. Operational Safety vs Adaptability</h3>\n<p>Partition-based design:</p>\n<ul>\n<li>Safer by default</li>\n<li>Enforced by the OS</li>\n</ul>\n<p>Quota-based design:</p>\n<ul>\n<li>Requires correct configuration</li>\n<li>Depends on operational discipline</li>\n</ul>\n<p>We accepted this trade-off because:</p>\n<blockquote>\n<p>Our system is still evolving, and adaptability is more valuable than strict enforcement at this stage.</p>\n</blockquote>\n<hr>\n<h3>3. Observability vs Control</h3>\n<p>Partition split:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">df</span> -h\n</code></pre>\n<p>→ Immediate visibility</p>\n<p>Quota-based:</p>\n<pre><code class=\"hljs language-arduino\">xfs_quota -x -c <span class=\"hljs-string\">\"report\"</span>\n</code></pre>\n<p>→ Requires tooling</p>\n<p>We accept slightly reduced observability in exchange for:</p>\n<ul>\n<li>Dynamic capacity management</li>\n<li>Avoiding premature optimization</li>\n</ul>\n<hr>\n<h3>4. Production Convention vs Practical Engineering</h3>\n<p>Typical production clusters use:</p>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate mount points.</p>\n<p>We intentionally diverge because:</p>\n<ul>\n<li>This is an early-stage system</li>\n<li>We prioritize iteration speed over convention</li>\n<li>We plan to evolve toward stricter isolation later if needed</li>\n</ul>\n<hr>\n<h1>Decision Rationale</h1>\n<p>We chose <strong>XFS + project quota</strong> based on the following reasoning:</p>\n<hr>\n<h2>1. Workload is large-file and throughput-heavy</h2>\n<p>The system handles:</p>\n<ul>\n<li>Models (GB–TB scale)</li>\n<li>Datasets</li>\n<li>Checkpoints</li>\n<li>Container layers</li>\n</ul>\n<p>XFS is well-suited for:</p>\n<ul>\n<li>Sequential I/O</li>\n<li>Large file handling</li>\n<li>High throughput operations</li>\n</ul>\n<hr>\n<h2>2. Need for dynamic capacity allocation</h2>\n<p>We do not yet know:</p>\n<ul>\n<li>Exact ratio of persistent vs temporary data</li>\n<li>Future workload patterns</li>\n</ul>\n<p>Using pquota allows:</p>\n<ul>\n<li>Adjusting limits without repartitioning</li>\n<li>Avoiding irreversible decisions</li>\n</ul>\n<hr>\n<h2>3. Scaling strategy does not depend on shrinking</h2>\n<p>We explicitly decided:</p>\n<ul>\n<li>\n<p>No reliance on shrinking volumes</p>\n</li>\n<li>\n<p>Scaling will be done by:</p>\n<ul>\n<li>Adding SSDs</li>\n<li>Integrating NAS</li>\n</ul>\n</li>\n</ul>\n<p>This removes a key disadvantage of XFS.</p>\n<hr>\n<h2>4. Early-stage system prioritizes iteration</h2>\n<p>At this stage:</p>\n<ul>\n<li>Requirements are evolving</li>\n<li>Usage patterns are not fully stable</li>\n</ul>\n<p>We optimize for:</p>\n<blockquote>\n<p>Speed of iteration over rigid correctness</p>\n</blockquote>\n<hr>\n<h2>5. Acceptable level of risk</h2>\n<p>We acknowledge:</p>\n<ul>\n<li>Quota is not as strong as partition isolation</li>\n<li>Misconfiguration is possible</li>\n</ul>\n<p>But we mitigate this by:</p>\n<ul>\n<li>Clear directory structure</li>\n<li>Explicit usage guidelines</li>\n<li>Monitoring and documentation</li>\n</ul>\n<hr>\n<h1>Final Layout</h1>\n<pre><code class=\"hljs language-bash\">/            → ext4 (system)\n/data        → XFS\n  /persistent\n  /scratch\n</code></pre>\n<p>Quota is enforced via:</p>\n<ul>\n<li>XFS project quota</li>\n<li>Separate limits for persistent and scratch</li>\n</ul>\n<hr>\n<h1>Operational Guidelines</h1>\n<h2>Directory Usage</h2>\n<h3>Persistent Data</h3>\n<pre><code class=\"hljs language-bash\">/data/persistent\n</code></pre>\n<ul>\n<li>models</li>\n<li>datasets</li>\n<li>repositories</li>\n<li>shared artifacts</li>\n<li>Docker root</li>\n</ul>\n<hr>\n<h3>Temporary Data</h3>\n<pre><code class=\"hljs language-bash\">/data/scratch\n</code></pre>\n<ul>\n<li>checkpoints</li>\n<li>cache</li>\n<li>build artifacts</li>\n<li>intermediate outputs</li>\n</ul>\n<hr>\n<h2>Slurm Example</h2>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">export</span> TMPDIR=/data/scratch/<span class=\"hljs-variable\">$USER</span>/<span class=\"hljs-variable\">$SLURM_JOB_ID</span>\n<span class=\"hljs-built_in\">mkdir</span> -p <span class=\"hljs-variable\">$TMPDIR</span>\n</code></pre>\n<hr>\n<h2>Docker</h2>\n<p>Docker root should be moved to:</p>\n<pre><code class=\"hljs language-bash\">/data/persistent/docker\n</code></pre>\n<hr>\n<h1>Future Evolution</h1>\n<p>This design is not final.</p>\n<p>Possible future changes:</p>\n<ul>\n<li>Split <code>/data</code> and <code>/scratch</code> into separate volumes</li>\n<li>Introduce network storage (NAS / distributed FS)</li>\n<li>Move heavy workloads to dedicated compute nodes</li>\n</ul>\n<hr>\n<h1>Lessons Learned</h1>\n<ol>\n<li>Storage design is about trade-offs, not absolutes</li>\n<li>Filesystem choice must match workload characteristics</li>\n<li>Early-stage systems benefit from flexibility</li>\n<li>Isolation can be introduced later, but rigidity is hard to undo</li>\n<li>Production design evolves — it is not fixed from day one</li>\n</ol>\n<hr>\n<h1>Conclusion</h1>\n<p>We chose <strong>XFS + project quota</strong> because it provides:</p>\n<ul>\n<li>High performance for data-heavy workloads</li>\n<li>Flexibility during early-stage development</li>\n<li>A balance between structure and adaptability</li>\n</ul>\n<blockquote>\n<p>Instead of optimizing for a perfect final state, we optimized for a system that can evolve.</p>\n</blockquote>"
  },
  {
    "slug": "042326_LinkdeIn_Repost_Marker",
    "title": "042326 LinkdeIn Repost Marker",
    "date": "",
    "summary": "Building a LinkedIn Reposted Marker Extension: DOM Detection, Background Prefetch, and Defensive Browser Engineering [GitHub Link](https://github.com/pinkpig777/reposted marker) In…",
    "tags": [],
    "html": "<h1>Building a LinkedIn Reposted Marker Extension: DOM Detection, Background Prefetch, and Defensive Browser Engineering</h1>\n<h2><a href=\"https://github.com/pinkpig777/reposted-marker\" target=\"_blank\" rel=\"noopener noreferrer\">GitHub Link</a></h2>\n<h2>Introduction</h2>\n<p>Most browser extensions look simple from the outside and messy from the inside.</p>\n<p>This one is a good example.</p>\n<p>The goal of <strong>LinkedIn Reposted Marker</strong> is straightforward: detect reposted job listings on LinkedIn and surface that signal directly in the jobs list, not only inside the detail panel after a click.</p>\n<p>In practice, that means solving a cluster of problems that show up in almost every serious browser-extension project:</p>\n<ul>\n<li>the target page is a dynamic single-page app</li>\n<li>the DOM changes frequently and unpredictably</li>\n<li>the useful signal is not always visible where the user needs it</li>\n<li>route shape and DOM shape are not always aligned</li>\n<li>background work has to be fast enough to feel helpful, but conservative enough to avoid rate limits</li>\n</ul>\n<p>This post walks through the technical design of the extension, the architecture decisions behind it, and the implementation lessons that came from making it work reliably.</p>\n<h2>The Product Problem</h2>\n<p>LinkedIn often exposes the reposted signal most clearly inside the job detail panel, but the real user need exists one step earlier:</p>\n<blockquote>\n<p>“Can I tell this is a reposted job before I spend attention opening it?”</p>\n</blockquote>\n<p>That changes the technical problem.</p>\n<p>A basic DOM-only extension could highlight reposted text if it is already visible in the left-hand list. But a useful version has to go further:</p>\n<ol>\n<li>detect reposted status from visible DOM when possible</li>\n<li>infer it from the open detail panel when the user clicks into a job</li>\n<li>prefetch unresolved jobs in the background so the left list improves over time</li>\n<li>keep everything stable while LinkedIn rerenders the page during scroll, route changes, and async updates</li>\n</ol>\n<p>That is where the extension architecture comes from.</p>\n<h2>High-Level Architecture</h2>\n<p>The extension has four major runtime pieces:</p>\n<ol>\n<li>\n<p><strong>Content runtime</strong>\nThis runs on supported LinkedIn jobs pages. It scans job cards, detects visible reposted signals, maps job cards to job IDs, and applies highlighting to both the list and the detail panel.</p>\n</li>\n<li>\n<p><strong>Background worker</strong>\nThis handles asynchronous prefetch work for unresolved jobs. It validates incoming requests, applies queue policy, fetches LinkedIn job pages, and returns results to the content runtime.</p>\n</li>\n<li>\n<p><strong>Shared policy layer</strong>\nThis centralizes route support, payload validation, cache replacement rules, timing constants, and debug log behavior so that content and background logic stay consistent.</p>\n</li>\n<li>\n<p><strong>Popup control menu</strong>\nThis gives the user runtime controls and visibility into system state: enable/disable behavior, prefetch tuning, cache TTL, queue diagnostics, cache clearing, and debug log export.</p>\n</li>\n</ol>\n<p>At a product level, the runtime flow looks like this:</p>\n<div class=\"mermaid\">flowchart LR\n  A[\"LinkedIn Jobs Page\"] --> B[\"Extension Content Runtime\"]\n  B --> C[\"Visible-page detection\"]\n  B --> D[\"Background prefetch for unresolved jobs\"]\n\n  D --> E[\"Background Worker\"]\n  E --> F[\"Fetch + classify job status\"]\n  E --> G[\"Cache results\"]\n\n  G --> H[\"Return results to the page\"]\n  H --> I[\"Highlight left list and detail panel\"]\n\n  J[\"Popup Control Menu\"] --> K[\"Settings and diagnostics\"]\n  K --> B\n  K --> E\n  K --> G\n</div>\n<p>The key design choice is that <strong>background prefetch augments DOM detection instead of replacing it</strong>. The extension remains useful even if background fetch is disabled or temporarily rate-limited.</p>\n<h2>Why DOM-First Was the Right Starting Point</h2>\n<p>A lot of extension projects fail because they start with the most “clever” part first.</p>\n<p>For this extension, the tempting path was to jump immediately into background fetch and indirect classification. That would have been a mistake.</p>\n<p>The DOM-first model was the correct foundation for three reasons:</p>\n<ol>\n<li>\n<p>It validates the user experience early.\nIf highlighting reposted jobs is not visually useful in the left list and detail panel, the rest of the system is irrelevant.</p>\n</li>\n<li>\n<p>It establishes a stable mapping model.\nBefore doing background work, the extension needs to know what a “job card” actually is, how to locate it, how to identify it, and how to update it later.</p>\n</li>\n<li>\n<p>It creates a fallback path.\nBackground prefetch is inherently more fragile because it depends on network behavior, route support, throttling, and LinkedIn response shape. DOM detection provides graceful degradation.</p>\n</li>\n</ol>\n<p>That is why the project evolved in phases: visible detection first, then job identity and cache, then background prefetch, then performance controls.</p>\n<h2>The Real Difficulty: LinkedIn Is Not a Stable Document</h2>\n<p>The hardest part of this extension is not matching the word <code>Reposted</code>.</p>\n<p>It is handling LinkedIn as a dynamic application rather than a static page.</p>\n<p>The implementation has to survive:</p>\n<ul>\n<li>infinite scroll</li>\n<li>async DOM insertion</li>\n<li>rerendered card containers</li>\n<li>route changes without full reloads</li>\n<li>different page variants such as <code>/jobs/search/</code> and <code>/jobs/search-results/</code></li>\n</ul>\n<p>That is why the content runtime uses:</p>\n<ul>\n<li>a debounced <code>MutationObserver</code></li>\n<li>scroll-driven rescanning</li>\n<li>route polling</li>\n<li>data attributes to track extension-owned state</li>\n<li>a card registry keyed by job ID</li>\n</ul>\n<p>Without that, the extension would either miss updates or re-apply work constantly and create flicker.</p>\n<h2>Job Identity: The Backbone of Everything</h2>\n<p>Once the extension moved beyond purely visible DOM detection, <strong>job identity became the critical internal primitive</strong>.</p>\n<p>If a job can be reliably represented by a stable <code>jobId</code>, then the extension can:</p>\n<ul>\n<li>cache results</li>\n<li>reapply known status to rerendered cards</li>\n<li>connect detail-panel detection back to the left-side card</li>\n<li>deduplicate background queue work</li>\n<li>resolve later async results against the correct DOM node</li>\n</ul>\n<p>The implementation extracts job IDs from multiple places because LinkedIn does not always expose them in the same way:</p>\n<ul>\n<li><code>/jobs/view/&#x3C;id></code> URL paths</li>\n<li><code>currentJobId</code> query parameters</li>\n<li><code>data-*</code> attributes and URN-like fields in the DOM</li>\n</ul>\n<p>This ended up mattering a lot in <code>search-results</code> routes, where URL shape and selected-card behavior differ from the simpler <code>/jobs/search/</code> flow.</p>\n<p>One of the clearest lessons from the project is this:</p>\n<blockquote>\n<p>If the job identity layer is weak, everything above it becomes probabilistic.</p>\n</blockquote>\n<h2>Background Prefetch: Useful, but Dangerous</h2>\n<p>Background prefetch is the feature that makes the extension genuinely better than a simple UI highlighter.</p>\n<p>It allows the left list to improve asynchronously as unresolved jobs are fetched and classified.</p>\n<p>But background prefetch also introduces the system’s biggest operational risk: <strong>sending too many requests</strong>.</p>\n<p>That risk showed up in practice through HTTP <code>429 Too Many Requests</code> responses. The fix was not just “slow it down a bit.” It required an explicit queue model and retry policy:</p>\n<ul>\n<li>bounded pending queue</li>\n<li>configurable concurrency</li>\n<li>minimum interval between requests</li>\n<li>pause window on rate limiting</li>\n<li>cooldowns for <code>error</code> and <code>rate_limited</code> states</li>\n<li>cache reuse before issuing new work</li>\n<li>viewport-aware candidate selection so the extension only prefetches nearby jobs</li>\n</ul>\n<p>This turned prefetch from an eager fire-and-forget feature into a managed subsystem.</p>\n<p>That is an important engineering distinction.</p>\n<p>The question is not:</p>\n<blockquote>\n<p>“Can we fetch unresolved jobs in the background?”</p>\n</blockquote>\n<p>The real question is:</p>\n<blockquote>\n<p>“Can we do it predictably, safely, and in a way that still feels responsive?”</p>\n</blockquote>\n<h2>Viewport Windowing and Queue Control</h2>\n<p>After basic prefetch was working, performance became the next problem.</p>\n<p>If every unresolved card on a long job list can be queued immediately, then the extension is technically functioning but operationally wrong. It wastes work on jobs the user may never see, increases rate-limit risk, and makes queue behavior harder to reason about.</p>\n<p>That led to a more disciplined model:</p>\n<ul>\n<li>only queue jobs near the viewport</li>\n<li>score candidates by distance from the viewport</li>\n<li>prioritize closer cards first</li>\n<li>cap the maximum pending queue size</li>\n<li>opportunistically refresh stale cached results for nearby jobs only</li>\n</ul>\n<p>This is a common pattern in systems that look UI-driven but actually need resource scheduling.</p>\n<p>The extension does not just detect jobs. It also decides <strong>which unresolved jobs are worth spending background budget on right now</strong>.</p>\n<h2>Route Support: Saying “No” Is Part of Reliability</h2>\n<p>One of the cleaner engineering decisions in the codebase is explicit route support.</p>\n<p>The extension does not pretend every LinkedIn Jobs route is equally supported. Route validation is centralized in a shared contracts layer, and unsupported pages are intentionally gated off.</p>\n<p>At the moment:</p>\n<ul>\n<li><code>/jobs/search/</code> is supported</li>\n<li><code>/jobs/search-results/</code> has historically been trickier and required multiple hardening passes</li>\n<li>unsupported routes are surfaced in the popup instead of silently misbehaving</li>\n</ul>\n<p>This matters because many browser extensions fail in a subtle way: they partially work on pages they were never really designed for.</p>\n<p>The better behavior is:</p>\n<ol>\n<li>be explicit about what is supported</li>\n<li>enforce that contract in both content and background boundaries</li>\n<li>expose support state to the user</li>\n</ol>\n<p>That reduces ambiguity and makes debugging much simpler.</p>\n<h2>Popup Controls and Runtime Introspection</h2>\n<p>The popup is not just a settings page. It is also an operator console.</p>\n<p>The extension exposes:</p>\n<ul>\n<li>enable/disable toggle</li>\n<li>background prefetch toggle</li>\n<li>left-list and detail-panel marking toggles</li>\n<li>prefetch window</li>\n<li>prefetch concurrency</li>\n<li>cache TTL</li>\n<li>page support state</li>\n<li>queue state</li>\n<li>cache state</li>\n<li>cache clearing</li>\n<li>debug log export</li>\n</ul>\n<p>This is useful for two reasons.</p>\n<p>First, it makes the extension practical for real users. They can tune aggressiveness or turn off behavior they do not want.</p>\n<p>Second, it makes the system diagnosable. That matters because a browser extension has two audiences:</p>\n<ul>\n<li>the end user</li>\n<li>the maintainer trying to understand why something did not update correctly</li>\n</ul>\n<p>Those needs often overlap.</p>\n<h2>Debug Logging as a Product Feature</h2>\n<p>This project ended up needing more runtime visibility than a few <code>console.log</code> statements could provide.</p>\n<p>A shared debug log was added so that event history can be stored locally and exported as JSON from the popup.</p>\n<p>That log captures things like:</p>\n<ul>\n<li>settings changes</li>\n<li>content runtime initialization</li>\n<li>queue enqueue / release / drop behavior</li>\n<li>fetch completion and rate-limiting events</li>\n<li>status result delivery back to the content runtime</li>\n</ul>\n<p>That may sound like internal tooling, but for a browser extension it becomes part of the product quality story.</p>\n<p>The more dynamic and asynchronous the system becomes, the more valuable it is to have a durable local event trail instead of trying to reconstruct behavior from a live DevTools session.</p>\n<h2>The Hardest Bugs Were Mapping Bugs</h2>\n<p>The most persistent issues were not styling bugs. They were mapping bugs.</p>\n<p>The extension repeatedly ran into variations of this failure mode:</p>\n<ul>\n<li>the right-side detail panel correctly shows a reposted signal</li>\n<li>the left-side card should be updated</li>\n<li>but the extension cannot confidently map the detail result back to the correct card</li>\n</ul>\n<p>Several hardening steps came out of that:</p>\n<ul>\n<li>support multiple job ID sources</li>\n<li>treat <code>search-results</code> as a special route family</li>\n<li>use selected-card heuristics when route behavior differs</li>\n<li>fall back to title/company matching when job ID mapping is weak</li>\n<li>avoid accidentally treating detail DOM nodes as left-list cards</li>\n</ul>\n<p>This is a good reminder that in UI-heavy browser extensions, <strong>state synchronization bugs often matter more than detection bugs</strong>.</p>\n<p>Finding the signal is only half the problem. Applying it to the right place is the real challenge.</p>\n<h2>Why This Architecture Works</h2>\n<p>The current architecture works because it is layered in a disciplined way:</p>\n<ul>\n<li><strong>DOM detection</strong> handles the obvious cases cheaply</li>\n<li><strong>job identity and registry</strong> stabilize updates across rerenders</li>\n<li><strong>background prefetch</strong> improves coverage</li>\n<li><strong>cache policy</strong> prevents repeated work and controls replacement</li>\n<li><strong>queue policy</strong> prevents the prefetch subsystem from becoming unstable</li>\n<li><strong>popup controls and debug logs</strong> make the runtime observable and tunable</li>\n</ul>\n<p>None of those pieces are individually surprising.</p>\n<p>What matters is that they cooperate under the real constraints of a modern browser extension:</p>\n<ul>\n<li>unstable target DOM</li>\n<li>asynchronous UI updates</li>\n<li>multiple runtime contexts</li>\n<li>background request limits</li>\n<li>user-facing latency expectations</li>\n</ul>\n<h2>Lessons for Other Browser Extensions</h2>\n<p>If you are building a browser extension against a large SPA, the most transferable lessons from this project are:</p>\n<h3>1. Start with the user-visible core</h3>\n<p>Do the simplest useful thing first. In this project, that was visible DOM detection and highlighting.</p>\n<h3>2. Treat identity as infrastructure</h3>\n<p>If the system needs to reconnect async work back to the DOM later, stable identity is not optional.</p>\n<h3>3. Build background work as a controlled subsystem</h3>\n<p>Anything that fetches in the background needs queue policy, cache policy, and rate-limit behavior from the start.</p>\n<h3>4. Be explicit about support boundaries</h3>\n<p>A route support matrix is better than quietly “kind of” supporting everything.</p>\n<h3>5. Observability is worth it even in a small extension</h3>\n<p>Once the system spans popup, content runtime, background worker, and storage, debugging without durable logs becomes unnecessarily painful.</p>\n<h2>Closing Thoughts</h2>\n<p>LinkedIn Reposted Marker started as a focused utility, but it quickly turned into a compact example of defensive extension engineering.</p>\n<p>The interesting part is not that it can highlight reposted jobs. The interesting part is how much supporting architecture is required to make that simple feature feel reliable on top of a dynamic site that does not particularly want to be scraped, inferred, or stabilized.</p>\n<p>That is the real work in browser extensions:</p>\n<p>not just adding behavior to a page, but building a small, resilient system that can keep doing the right thing while the page changes underneath it.</p>"
  }
];

export const blogPostBySlug: Record<string, BlogPost> = {
  "042126_GPU_Cluster_Storage_Design": {
    "slug": "042126_GPU_Cluster_Storage_Design",
    "title": "042126 GPU Cluster Storage Design",
    "date": "",
    "summary": "Storage Design for a Hybrid Login + Compute Node (Cluster Setup) Overview This document captures the reasoning, design decisions, and implementation details for setting up storage …",
    "tags": [],
    "html": "<h1>Storage Design for a Hybrid Login + Compute Node (Cluster Setup)</h1>\n<h2>Overview</h2>\n<p>This document captures the reasoning, design decisions, and implementation details for setting up storage on a GPU server that initially serves as both a <strong>login node</strong> and a <strong>compute node</strong>, with future expansion into a multi-node cluster.</p>\n<p>Instead of following a rigid \"textbook\" layout, we intentionally designed a system that balances:</p>\n<ul>\n<li>Performance</li>\n<li>Flexibility</li>\n<li>Operational simplicity</li>\n<li>Future scalability</li>\n</ul>\n<hr>\n<h1>Key Concepts</h1>\n<h2>LVM (Logical Volume Manager)</h2>\n<p>LVM provides a flexible abstraction layer over physical storage:</p>\n<pre><code class=\"hljs\">Physical Disk → PV → VG → LV\n</code></pre>\n<p>It allows:</p>\n<ul>\n<li>Pooling storage into a single volume group</li>\n<li>Dynamically allocating logical volumes</li>\n<li>Future expansion by adding disks</li>\n</ul>\n<p>However:</p>\n<blockquote>\n<p>LVM does not eliminate filesystem constraints (e.g., XFS cannot shrink)</p>\n</blockquote>\n<hr>\n<h2>ext4</h2>\n<p>A general-purpose filesystem focused on:</p>\n<ul>\n<li>Stability</li>\n<li>Simplicity</li>\n<li>Mature recovery tooling</li>\n</ul>\n<p>Best suited for:</p>\n<ul>\n<li>System partitions</li>\n<li>Conservative environments</li>\n</ul>\n<hr>\n<h2>XFS</h2>\n<p>A high-performance filesystem designed for:</p>\n<ul>\n<li>Large files</li>\n<li>High throughput</li>\n<li>Parallel I/O workloads</li>\n</ul>\n<p>Key characteristics:</p>\n<ul>\n<li>Excellent for data-intensive systems</li>\n<li>Widely used in production clusters</li>\n<li>Supports project quota (pquota)</li>\n</ul>\n<p>Limitations:</p>\n<ul>\n<li>Cannot shrink volumes</li>\n<li>Requires more deliberate planning</li>\n</ul>\n<hr>\n<h2>Project Quota (pquota)</h2>\n<p>XFS provides <strong>project-based quota</strong>, which allows limiting disk usage at the directory level.</p>\n<p>Unlike traditional user/group quota:</p>\n<ul>\n<li>Applies to directory trees</li>\n<li>Suitable for workload isolation (e.g., <code>/data</code> vs <code>/scratch</code>)</li>\n</ul>\n<hr>\n<h1>Problem Context</h1>\n<p>We are working with:</p>\n<ul>\n<li>~2TB local NVMe storage</li>\n<li>200GB already allocated for system (<code>/</code>)</li>\n<li>~1.8TB remaining</li>\n</ul>\n<p>Node role:</p>\n<ul>\n<li>Login node (interactive usage)</li>\n<li>Compute node (jobs, experiments)</li>\n</ul>\n<p>Future direction:</p>\n<ul>\n<li>Expand into multi-node cluster</li>\n<li>Add storage via SSD or NAS</li>\n<li>Avoid reliance on resizing/shrinking partitions</li>\n</ul>\n<hr>\n<h1>Design Options Considered</h1>\n<h2>Option 1: Hard Partition / LVM Split</h2>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate logical volumes for:</p>\n<ul>\n<li>Persistent data</li>\n<li>Temporary data</li>\n</ul>\n<h3>Pros</h3>\n<ul>\n<li>Strong isolation</li>\n<li>Easy observability (<code>df -h</code>)</li>\n<li>Clear mental model</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>Requires early capacity decisions</li>\n<li>XFS cannot shrink → misallocation is costly</li>\n<li>Less flexible during early-stage usage</li>\n</ul>\n<hr>\n<h2>Option 2: Directory-based Separation Only</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/scratch\n</code></pre>\n<h3>Pros</h3>\n<ul>\n<li>Simple</li>\n<li>No upfront partitioning decisions</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>No isolation</li>\n<li>High risk of disk exhaustion</li>\n<li>Not suitable for multi-user environments</li>\n</ul>\n<hr>\n<h2>Option 3: Single XFS + Project Quota (<strong>Chosen</strong>)</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/persistent\n/data/scratch\n</code></pre>\n<p>Use:</p>\n<ul>\n<li>XFS as the underlying filesystem</li>\n<li>Project quota (pquota) to enforce limits</li>\n</ul>\n<hr>\n<h1>Decision Trade-offs</h1>\n<p>This decision is fundamentally a trade-off between:</p>\n<h3>1. Isolation vs Flexibility</h3>\n<table>\n<thead>\n<tr>\n<th>Approach</th>\n<th>Isolation</th>\n<th>Flexibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Partition split</td>\n<td>Strong</td>\n<td>Low</td>\n</tr>\n<tr>\n<td>XFS + pquota</td>\n<td>Medium</td>\n<td>High</td>\n</tr>\n</tbody>\n</table>\n<p>We chose <strong>flexibility</strong> because:</p>\n<ul>\n<li>Early-stage workload is uncertain</li>\n<li>Data vs scratch usage ratio may evolve</li>\n<li>Repartitioning later is costly</li>\n</ul>\n<hr>\n<h3>2. Operational Safety vs Adaptability</h3>\n<p>Partition-based design:</p>\n<ul>\n<li>Safer by default</li>\n<li>Enforced by the OS</li>\n</ul>\n<p>Quota-based design:</p>\n<ul>\n<li>Requires correct configuration</li>\n<li>Depends on operational discipline</li>\n</ul>\n<p>We accepted this trade-off because:</p>\n<blockquote>\n<p>Our system is still evolving, and adaptability is more valuable than strict enforcement at this stage.</p>\n</blockquote>\n<hr>\n<h3>3. Observability vs Control</h3>\n<p>Partition split:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">df</span> -h\n</code></pre>\n<p>→ Immediate visibility</p>\n<p>Quota-based:</p>\n<pre><code class=\"hljs language-arduino\">xfs_quota -x -c <span class=\"hljs-string\">\"report\"</span>\n</code></pre>\n<p>→ Requires tooling</p>\n<p>We accept slightly reduced observability in exchange for:</p>\n<ul>\n<li>Dynamic capacity management</li>\n<li>Avoiding premature optimization</li>\n</ul>\n<hr>\n<h3>4. Production Convention vs Practical Engineering</h3>\n<p>Typical production clusters use:</p>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate mount points.</p>\n<p>We intentionally diverge because:</p>\n<ul>\n<li>This is an early-stage system</li>\n<li>We prioritize iteration speed over convention</li>\n<li>We plan to evolve toward stricter isolation later if needed</li>\n</ul>\n<hr>\n<h1>Decision Rationale</h1>\n<p>We chose <strong>XFS + project quota</strong> based on the following reasoning:</p>\n<hr>\n<h2>1. Workload is large-file and throughput-heavy</h2>\n<p>The system handles:</p>\n<ul>\n<li>Models (GB–TB scale)</li>\n<li>Datasets</li>\n<li>Checkpoints</li>\n<li>Container layers</li>\n</ul>\n<p>XFS is well-suited for:</p>\n<ul>\n<li>Sequential I/O</li>\n<li>Large file handling</li>\n<li>High throughput operations</li>\n</ul>\n<hr>\n<h2>2. Need for dynamic capacity allocation</h2>\n<p>We do not yet know:</p>\n<ul>\n<li>Exact ratio of persistent vs temporary data</li>\n<li>Future workload patterns</li>\n</ul>\n<p>Using pquota allows:</p>\n<ul>\n<li>Adjusting limits without repartitioning</li>\n<li>Avoiding irreversible decisions</li>\n</ul>\n<hr>\n<h2>3. Scaling strategy does not depend on shrinking</h2>\n<p>We explicitly decided:</p>\n<ul>\n<li>\n<p>No reliance on shrinking volumes</p>\n</li>\n<li>\n<p>Scaling will be done by:</p>\n<ul>\n<li>Adding SSDs</li>\n<li>Integrating NAS</li>\n</ul>\n</li>\n</ul>\n<p>This removes a key disadvantage of XFS.</p>\n<hr>\n<h2>4. Early-stage system prioritizes iteration</h2>\n<p>At this stage:</p>\n<ul>\n<li>Requirements are evolving</li>\n<li>Usage patterns are not fully stable</li>\n</ul>\n<p>We optimize for:</p>\n<blockquote>\n<p>Speed of iteration over rigid correctness</p>\n</blockquote>\n<hr>\n<h2>5. Acceptable level of risk</h2>\n<p>We acknowledge:</p>\n<ul>\n<li>Quota is not as strong as partition isolation</li>\n<li>Misconfiguration is possible</li>\n</ul>\n<p>But we mitigate this by:</p>\n<ul>\n<li>Clear directory structure</li>\n<li>Explicit usage guidelines</li>\n<li>Monitoring and documentation</li>\n</ul>\n<hr>\n<h1>Final Layout</h1>\n<pre><code class=\"hljs language-bash\">/            → ext4 (system)\n/data        → XFS\n  /persistent\n  /scratch\n</code></pre>\n<p>Quota is enforced via:</p>\n<ul>\n<li>XFS project quota</li>\n<li>Separate limits for persistent and scratch</li>\n</ul>\n<hr>\n<h1>Operational Guidelines</h1>\n<h2>Directory Usage</h2>\n<h3>Persistent Data</h3>\n<pre><code class=\"hljs language-bash\">/data/persistent\n</code></pre>\n<ul>\n<li>models</li>\n<li>datasets</li>\n<li>repositories</li>\n<li>shared artifacts</li>\n<li>Docker root</li>\n</ul>\n<hr>\n<h3>Temporary Data</h3>\n<pre><code class=\"hljs language-bash\">/data/scratch\n</code></pre>\n<ul>\n<li>checkpoints</li>\n<li>cache</li>\n<li>build artifacts</li>\n<li>intermediate outputs</li>\n</ul>\n<hr>\n<h2>Slurm Example</h2>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">export</span> TMPDIR=/data/scratch/<span class=\"hljs-variable\">$USER</span>/<span class=\"hljs-variable\">$SLURM_JOB_ID</span>\n<span class=\"hljs-built_in\">mkdir</span> -p <span class=\"hljs-variable\">$TMPDIR</span>\n</code></pre>\n<hr>\n<h2>Docker</h2>\n<p>Docker root should be moved to:</p>\n<pre><code class=\"hljs language-bash\">/data/persistent/docker\n</code></pre>\n<hr>\n<h1>Future Evolution</h1>\n<p>This design is not final.</p>\n<p>Possible future changes:</p>\n<ul>\n<li>Split <code>/data</code> and <code>/scratch</code> into separate volumes</li>\n<li>Introduce network storage (NAS / distributed FS)</li>\n<li>Move heavy workloads to dedicated compute nodes</li>\n</ul>\n<hr>\n<h1>Lessons Learned</h1>\n<ol>\n<li>Storage design is about trade-offs, not absolutes</li>\n<li>Filesystem choice must match workload characteristics</li>\n<li>Early-stage systems benefit from flexibility</li>\n<li>Isolation can be introduced later, but rigidity is hard to undo</li>\n<li>Production design evolves — it is not fixed from day one</li>\n</ol>\n<hr>\n<h1>Conclusion</h1>\n<p>We chose <strong>XFS + project quota</strong> because it provides:</p>\n<ul>\n<li>High performance for data-heavy workloads</li>\n<li>Flexibility during early-stage development</li>\n<li>A balance between structure and adaptability</li>\n</ul>\n<blockquote>\n<p>Instead of optimizing for a perfect final state, we optimized for a system that can evolve.</p>\n</blockquote>"
  },
  "042326_LinkdeIn_Repost_Marker": {
    "slug": "042326_LinkdeIn_Repost_Marker",
    "title": "042326 LinkdeIn Repost Marker",
    "date": "",
    "summary": "Building a LinkedIn Reposted Marker Extension: DOM Detection, Background Prefetch, and Defensive Browser Engineering [GitHub Link](https://github.com/pinkpig777/reposted marker) In…",
    "tags": [],
    "html": "<h1>Building a LinkedIn Reposted Marker Extension: DOM Detection, Background Prefetch, and Defensive Browser Engineering</h1>\n<h2><a href=\"https://github.com/pinkpig777/reposted-marker\" target=\"_blank\" rel=\"noopener noreferrer\">GitHub Link</a></h2>\n<h2>Introduction</h2>\n<p>Most browser extensions look simple from the outside and messy from the inside.</p>\n<p>This one is a good example.</p>\n<p>The goal of <strong>LinkedIn Reposted Marker</strong> is straightforward: detect reposted job listings on LinkedIn and surface that signal directly in the jobs list, not only inside the detail panel after a click.</p>\n<p>In practice, that means solving a cluster of problems that show up in almost every serious browser-extension project:</p>\n<ul>\n<li>the target page is a dynamic single-page app</li>\n<li>the DOM changes frequently and unpredictably</li>\n<li>the useful signal is not always visible where the user needs it</li>\n<li>route shape and DOM shape are not always aligned</li>\n<li>background work has to be fast enough to feel helpful, but conservative enough to avoid rate limits</li>\n</ul>\n<p>This post walks through the technical design of the extension, the architecture decisions behind it, and the implementation lessons that came from making it work reliably.</p>\n<h2>The Product Problem</h2>\n<p>LinkedIn often exposes the reposted signal most clearly inside the job detail panel, but the real user need exists one step earlier:</p>\n<blockquote>\n<p>“Can I tell this is a reposted job before I spend attention opening it?”</p>\n</blockquote>\n<p>That changes the technical problem.</p>\n<p>A basic DOM-only extension could highlight reposted text if it is already visible in the left-hand list. But a useful version has to go further:</p>\n<ol>\n<li>detect reposted status from visible DOM when possible</li>\n<li>infer it from the open detail panel when the user clicks into a job</li>\n<li>prefetch unresolved jobs in the background so the left list improves over time</li>\n<li>keep everything stable while LinkedIn rerenders the page during scroll, route changes, and async updates</li>\n</ol>\n<p>That is where the extension architecture comes from.</p>\n<h2>High-Level Architecture</h2>\n<p>The extension has four major runtime pieces:</p>\n<ol>\n<li>\n<p><strong>Content runtime</strong>\nThis runs on supported LinkedIn jobs pages. It scans job cards, detects visible reposted signals, maps job cards to job IDs, and applies highlighting to both the list and the detail panel.</p>\n</li>\n<li>\n<p><strong>Background worker</strong>\nThis handles asynchronous prefetch work for unresolved jobs. It validates incoming requests, applies queue policy, fetches LinkedIn job pages, and returns results to the content runtime.</p>\n</li>\n<li>\n<p><strong>Shared policy layer</strong>\nThis centralizes route support, payload validation, cache replacement rules, timing constants, and debug log behavior so that content and background logic stay consistent.</p>\n</li>\n<li>\n<p><strong>Popup control menu</strong>\nThis gives the user runtime controls and visibility into system state: enable/disable behavior, prefetch tuning, cache TTL, queue diagnostics, cache clearing, and debug log export.</p>\n</li>\n</ol>\n<p>At a product level, the runtime flow looks like this:</p>\n<div class=\"mermaid\">flowchart LR\n  A[\"LinkedIn Jobs Page\"] --> B[\"Extension Content Runtime\"]\n  B --> C[\"Visible-page detection\"]\n  B --> D[\"Background prefetch for unresolved jobs\"]\n\n  D --> E[\"Background Worker\"]\n  E --> F[\"Fetch + classify job status\"]\n  E --> G[\"Cache results\"]\n\n  G --> H[\"Return results to the page\"]\n  H --> I[\"Highlight left list and detail panel\"]\n\n  J[\"Popup Control Menu\"] --> K[\"Settings and diagnostics\"]\n  K --> B\n  K --> E\n  K --> G\n</div>\n<p>The key design choice is that <strong>background prefetch augments DOM detection instead of replacing it</strong>. The extension remains useful even if background fetch is disabled or temporarily rate-limited.</p>\n<h2>Why DOM-First Was the Right Starting Point</h2>\n<p>A lot of extension projects fail because they start with the most “clever” part first.</p>\n<p>For this extension, the tempting path was to jump immediately into background fetch and indirect classification. That would have been a mistake.</p>\n<p>The DOM-first model was the correct foundation for three reasons:</p>\n<ol>\n<li>\n<p>It validates the user experience early.\nIf highlighting reposted jobs is not visually useful in the left list and detail panel, the rest of the system is irrelevant.</p>\n</li>\n<li>\n<p>It establishes a stable mapping model.\nBefore doing background work, the extension needs to know what a “job card” actually is, how to locate it, how to identify it, and how to update it later.</p>\n</li>\n<li>\n<p>It creates a fallback path.\nBackground prefetch is inherently more fragile because it depends on network behavior, route support, throttling, and LinkedIn response shape. DOM detection provides graceful degradation.</p>\n</li>\n</ol>\n<p>That is why the project evolved in phases: visible detection first, then job identity and cache, then background prefetch, then performance controls.</p>\n<h2>The Real Difficulty: LinkedIn Is Not a Stable Document</h2>\n<p>The hardest part of this extension is not matching the word <code>Reposted</code>.</p>\n<p>It is handling LinkedIn as a dynamic application rather than a static page.</p>\n<p>The implementation has to survive:</p>\n<ul>\n<li>infinite scroll</li>\n<li>async DOM insertion</li>\n<li>rerendered card containers</li>\n<li>route changes without full reloads</li>\n<li>different page variants such as <code>/jobs/search/</code> and <code>/jobs/search-results/</code></li>\n</ul>\n<p>That is why the content runtime uses:</p>\n<ul>\n<li>a debounced <code>MutationObserver</code></li>\n<li>scroll-driven rescanning</li>\n<li>route polling</li>\n<li>data attributes to track extension-owned state</li>\n<li>a card registry keyed by job ID</li>\n</ul>\n<p>Without that, the extension would either miss updates or re-apply work constantly and create flicker.</p>\n<h2>Job Identity: The Backbone of Everything</h2>\n<p>Once the extension moved beyond purely visible DOM detection, <strong>job identity became the critical internal primitive</strong>.</p>\n<p>If a job can be reliably represented by a stable <code>jobId</code>, then the extension can:</p>\n<ul>\n<li>cache results</li>\n<li>reapply known status to rerendered cards</li>\n<li>connect detail-panel detection back to the left-side card</li>\n<li>deduplicate background queue work</li>\n<li>resolve later async results against the correct DOM node</li>\n</ul>\n<p>The implementation extracts job IDs from multiple places because LinkedIn does not always expose them in the same way:</p>\n<ul>\n<li><code>/jobs/view/&#x3C;id></code> URL paths</li>\n<li><code>currentJobId</code> query parameters</li>\n<li><code>data-*</code> attributes and URN-like fields in the DOM</li>\n</ul>\n<p>This ended up mattering a lot in <code>search-results</code> routes, where URL shape and selected-card behavior differ from the simpler <code>/jobs/search/</code> flow.</p>\n<p>One of the clearest lessons from the project is this:</p>\n<blockquote>\n<p>If the job identity layer is weak, everything above it becomes probabilistic.</p>\n</blockquote>\n<h2>Background Prefetch: Useful, but Dangerous</h2>\n<p>Background prefetch is the feature that makes the extension genuinely better than a simple UI highlighter.</p>\n<p>It allows the left list to improve asynchronously as unresolved jobs are fetched and classified.</p>\n<p>But background prefetch also introduces the system’s biggest operational risk: <strong>sending too many requests</strong>.</p>\n<p>That risk showed up in practice through HTTP <code>429 Too Many Requests</code> responses. The fix was not just “slow it down a bit.” It required an explicit queue model and retry policy:</p>\n<ul>\n<li>bounded pending queue</li>\n<li>configurable concurrency</li>\n<li>minimum interval between requests</li>\n<li>pause window on rate limiting</li>\n<li>cooldowns for <code>error</code> and <code>rate_limited</code> states</li>\n<li>cache reuse before issuing new work</li>\n<li>viewport-aware candidate selection so the extension only prefetches nearby jobs</li>\n</ul>\n<p>This turned prefetch from an eager fire-and-forget feature into a managed subsystem.</p>\n<p>That is an important engineering distinction.</p>\n<p>The question is not:</p>\n<blockquote>\n<p>“Can we fetch unresolved jobs in the background?”</p>\n</blockquote>\n<p>The real question is:</p>\n<blockquote>\n<p>“Can we do it predictably, safely, and in a way that still feels responsive?”</p>\n</blockquote>\n<h2>Viewport Windowing and Queue Control</h2>\n<p>After basic prefetch was working, performance became the next problem.</p>\n<p>If every unresolved card on a long job list can be queued immediately, then the extension is technically functioning but operationally wrong. It wastes work on jobs the user may never see, increases rate-limit risk, and makes queue behavior harder to reason about.</p>\n<p>That led to a more disciplined model:</p>\n<ul>\n<li>only queue jobs near the viewport</li>\n<li>score candidates by distance from the viewport</li>\n<li>prioritize closer cards first</li>\n<li>cap the maximum pending queue size</li>\n<li>opportunistically refresh stale cached results for nearby jobs only</li>\n</ul>\n<p>This is a common pattern in systems that look UI-driven but actually need resource scheduling.</p>\n<p>The extension does not just detect jobs. It also decides <strong>which unresolved jobs are worth spending background budget on right now</strong>.</p>\n<h2>Route Support: Saying “No” Is Part of Reliability</h2>\n<p>One of the cleaner engineering decisions in the codebase is explicit route support.</p>\n<p>The extension does not pretend every LinkedIn Jobs route is equally supported. Route validation is centralized in a shared contracts layer, and unsupported pages are intentionally gated off.</p>\n<p>At the moment:</p>\n<ul>\n<li><code>/jobs/search/</code> is supported</li>\n<li><code>/jobs/search-results/</code> has historically been trickier and required multiple hardening passes</li>\n<li>unsupported routes are surfaced in the popup instead of silently misbehaving</li>\n</ul>\n<p>This matters because many browser extensions fail in a subtle way: they partially work on pages they were never really designed for.</p>\n<p>The better behavior is:</p>\n<ol>\n<li>be explicit about what is supported</li>\n<li>enforce that contract in both content and background boundaries</li>\n<li>expose support state to the user</li>\n</ol>\n<p>That reduces ambiguity and makes debugging much simpler.</p>\n<h2>Popup Controls and Runtime Introspection</h2>\n<p>The popup is not just a settings page. It is also an operator console.</p>\n<p>The extension exposes:</p>\n<ul>\n<li>enable/disable toggle</li>\n<li>background prefetch toggle</li>\n<li>left-list and detail-panel marking toggles</li>\n<li>prefetch window</li>\n<li>prefetch concurrency</li>\n<li>cache TTL</li>\n<li>page support state</li>\n<li>queue state</li>\n<li>cache state</li>\n<li>cache clearing</li>\n<li>debug log export</li>\n</ul>\n<p>This is useful for two reasons.</p>\n<p>First, it makes the extension practical for real users. They can tune aggressiveness or turn off behavior they do not want.</p>\n<p>Second, it makes the system diagnosable. That matters because a browser extension has two audiences:</p>\n<ul>\n<li>the end user</li>\n<li>the maintainer trying to understand why something did not update correctly</li>\n</ul>\n<p>Those needs often overlap.</p>\n<h2>Debug Logging as a Product Feature</h2>\n<p>This project ended up needing more runtime visibility than a few <code>console.log</code> statements could provide.</p>\n<p>A shared debug log was added so that event history can be stored locally and exported as JSON from the popup.</p>\n<p>That log captures things like:</p>\n<ul>\n<li>settings changes</li>\n<li>content runtime initialization</li>\n<li>queue enqueue / release / drop behavior</li>\n<li>fetch completion and rate-limiting events</li>\n<li>status result delivery back to the content runtime</li>\n</ul>\n<p>That may sound like internal tooling, but for a browser extension it becomes part of the product quality story.</p>\n<p>The more dynamic and asynchronous the system becomes, the more valuable it is to have a durable local event trail instead of trying to reconstruct behavior from a live DevTools session.</p>\n<h2>The Hardest Bugs Were Mapping Bugs</h2>\n<p>The most persistent issues were not styling bugs. They were mapping bugs.</p>\n<p>The extension repeatedly ran into variations of this failure mode:</p>\n<ul>\n<li>the right-side detail panel correctly shows a reposted signal</li>\n<li>the left-side card should be updated</li>\n<li>but the extension cannot confidently map the detail result back to the correct card</li>\n</ul>\n<p>Several hardening steps came out of that:</p>\n<ul>\n<li>support multiple job ID sources</li>\n<li>treat <code>search-results</code> as a special route family</li>\n<li>use selected-card heuristics when route behavior differs</li>\n<li>fall back to title/company matching when job ID mapping is weak</li>\n<li>avoid accidentally treating detail DOM nodes as left-list cards</li>\n</ul>\n<p>This is a good reminder that in UI-heavy browser extensions, <strong>state synchronization bugs often matter more than detection bugs</strong>.</p>\n<p>Finding the signal is only half the problem. Applying it to the right place is the real challenge.</p>\n<h2>Why This Architecture Works</h2>\n<p>The current architecture works because it is layered in a disciplined way:</p>\n<ul>\n<li><strong>DOM detection</strong> handles the obvious cases cheaply</li>\n<li><strong>job identity and registry</strong> stabilize updates across rerenders</li>\n<li><strong>background prefetch</strong> improves coverage</li>\n<li><strong>cache policy</strong> prevents repeated work and controls replacement</li>\n<li><strong>queue policy</strong> prevents the prefetch subsystem from becoming unstable</li>\n<li><strong>popup controls and debug logs</strong> make the runtime observable and tunable</li>\n</ul>\n<p>None of those pieces are individually surprising.</p>\n<p>What matters is that they cooperate under the real constraints of a modern browser extension:</p>\n<ul>\n<li>unstable target DOM</li>\n<li>asynchronous UI updates</li>\n<li>multiple runtime contexts</li>\n<li>background request limits</li>\n<li>user-facing latency expectations</li>\n</ul>\n<h2>Lessons for Other Browser Extensions</h2>\n<p>If you are building a browser extension against a large SPA, the most transferable lessons from this project are:</p>\n<h3>1. Start with the user-visible core</h3>\n<p>Do the simplest useful thing first. In this project, that was visible DOM detection and highlighting.</p>\n<h3>2. Treat identity as infrastructure</h3>\n<p>If the system needs to reconnect async work back to the DOM later, stable identity is not optional.</p>\n<h3>3. Build background work as a controlled subsystem</h3>\n<p>Anything that fetches in the background needs queue policy, cache policy, and rate-limit behavior from the start.</p>\n<h3>4. Be explicit about support boundaries</h3>\n<p>A route support matrix is better than quietly “kind of” supporting everything.</p>\n<h3>5. Observability is worth it even in a small extension</h3>\n<p>Once the system spans popup, content runtime, background worker, and storage, debugging without durable logs becomes unnecessarily painful.</p>\n<h2>Closing Thoughts</h2>\n<p>LinkedIn Reposted Marker started as a focused utility, but it quickly turned into a compact example of defensive extension engineering.</p>\n<p>The interesting part is not that it can highlight reposted jobs. The interesting part is how much supporting architecture is required to make that simple feature feel reliable on top of a dynamic site that does not particularly want to be scraped, inferred, or stabilized.</p>\n<p>That is the real work in browser extensions:</p>\n<p>not just adding behavior to a page, but building a small, resilient system that can keep doing the right thing while the page changes underneath it.</p>"
  }
};

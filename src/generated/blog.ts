/* This file is auto-generated by scripts/build-blog.mjs */

export type BlogPost = {
  slug: string;
  title: string;
  date: string;
  summary: string;
  tags: string[];
  html: string;
};

export const blogPosts: BlogPost[] = [
  {
    "slug": "linkedin-reposted-marker",
    "title": "Building a LinkedIn Reposted Marker Extension",
    "date": "2026-04-23",
    "summary": "Detect reposted jobs, prefetch unresolved listings, and keep LinkedIn highlights stable across route changes and rerenders.",
    "tags": [
      "Browser Extension",
      "LinkedIn"
    ],
    "html": "<p><a href=\"https://github.com/pinkpig777/reposted-marker\" target=\"_blank\" rel=\"noopener noreferrer\">GitHub Link</a></p>\n<h2>Introduction</h2>\n<p>Most browser extensions look simple from the outside.</p>\n<p>Then you open the codebase and realize the browser is basically a haunted house with event listeners.</p>\n<p><strong>LinkedIn Reposted Marker</strong> started with a simple goal: help users identify reposted LinkedIn jobs directly from the jobs list, before they waste time clicking into each post.</p>\n<p>The idea is straightforward:</p>\n<blockquote>\n<p>If a job is reposted, show that signal earlier.</p>\n</blockquote>\n<p>But building that reliably was not just a matter of finding the word <code>Reposted</code>.</p>\n<p>LinkedIn is a dynamic single-page app. The page changes while you scroll, cards rerender, routes change without full reloads, and useful information sometimes appears only in the detail panel after a job is opened.</p>\n<p>So this small extension slowly turned into a real browser-extension engineering problem:</p>\n<ul>\n<li>detect reposted jobs from visible DOM when possible</li>\n<li>connect detail-panel signals back to the correct list card</li>\n<li>prefetch unresolved jobs in the background</li>\n<li>avoid sending too many requests</li>\n<li>keep highlights stable across scrolls, route changes, and rerenders</li>\n<li>give the user enough controls and diagnostics to understand what is happening</li>\n</ul>\n<p>This post walks through how the extension is designed, why the architecture ended up this way, and what I learned from making it reliable enough to use.</p>\n<h2>The Product Problem</h2>\n<p>The core user problem is simple:</p>\n<blockquote>\n<p>“Can I know this job is reposted before I spend attention opening it?”</p>\n</blockquote>\n<p>LinkedIn may show the reposted signal inside the job detail panel, but by that point the user has already clicked.</p>\n<p>That is too late.</p>\n<p>A basic extension could highlight reposted text if it is already visible in the left-side job list. That is useful, but limited. A better version needs to do more:</p>\n<ol>\n<li>detect reposted status from visible job cards</li>\n<li>detect status from the opened job detail panel</li>\n<li>map the detail-panel result back to the correct left-side card</li>\n<li>prefetch unresolved jobs in the background</li>\n<li>keep the UI stable while LinkedIn constantly mutates the page, because apparently static HTML was too peaceful for humanity</li>\n</ol>\n<p>That product need shaped the whole architecture.</p>\n<h2>High-Level Architecture</h2>\n<p>The extension has four main runtime pieces:</p>\n<ol>\n<li>\n<p><strong>Content runtime</strong></p>\n<p>This runs directly on supported LinkedIn jobs pages. It scans job cards, extracts job IDs, detects visible reposted signals, and applies visual markers to both the left-side list and the right-side detail panel.</p>\n</li>\n<li>\n<p><strong>Background worker</strong></p>\n<p>This handles async prefetching. When the content script finds unresolved jobs, it can ask the background worker to fetch and classify those jobs without blocking the page.</p>\n</li>\n<li>\n<p><strong>Shared policy layer</strong></p>\n<p>This centralizes route support, payload validation, cache rules, timing constants, and debug behavior. Without this layer, content and background logic would slowly drift apart, because duplicated logic always finds a way to embarrass you later.</p>\n</li>\n<li>\n<p><strong>Popup control menu</strong></p>\n<p>This gives the user runtime controls and diagnostics: enable/disable behavior, prefetch tuning, cache TTL, queue state, cache clearing, and debug log export.</p>\n</li>\n</ol>\n<p>The runtime flow looks like this:</p>\n<div class=\"mermaid\">flowchart LR\n  A[\"LinkedIn Jobs Page\"] --> B[\"Extension Content Runtime\"]\n  B --> C[\"Visible DOM Detection\"]\n  B --> D[\"Background Prefetch for Unresolved Jobs\"]\n\n  D --> E[\"Background Worker\"]\n  E --> F[\"Fetch and Classify Job Status\"]\n  E --> G[\"Cache Results\"]\n\n  G --> H[\"Return Results to Content Runtime\"]\n  H --> I[\"Highlight List Cards and Detail Panel\"]\n\n  J[\"Popup Control Menu\"] --> K[\"Settings and Diagnostics\"]\n  K --> B\n  K --> E\n  K --> G\n</div>\n<p>The important design decision is this:</p>\n<blockquote>\n<p>Background prefetch should improve DOM detection, not replace it.</p>\n</blockquote>\n<p>That means the extension still works even if prefetch is disabled, paused, rate-limited, or temporarily unavailable.</p>\n<h2>Why I Started with DOM Detection</h2>\n<p>The most tempting path was to start with background fetching first.</p>\n<p>That would have been the fun, over-engineered version. Naturally, also the wrong starting point.</p>\n<p>The right starting point was visible DOM detection.</p>\n<p>There were three reasons.</p>\n<p>First, it validated the product quickly. If marking reposted jobs in the list was not actually useful, there was no point building a bigger system around it.</p>\n<p>Second, it forced me to solve the basic page-mapping problem. Before doing async work, the extension needed to understand what a job card is, how to find it, how to extract its identity, and how to update it later.</p>\n<p>Third, DOM detection gave the extension a fallback path. Background prefetch depends on network behavior, route support, cache state, and rate limits. DOM detection is cheaper, faster, and less fragile.</p>\n<p>So the project evolved in layers:</p>\n<ol>\n<li>visible detection</li>\n<li>stable job identity</li>\n<li>cache and registry</li>\n<li>background prefetch</li>\n<li>queue control</li>\n<li>popup controls and debug logging</li>\n</ol>\n<p>That order mattered.</p>\n<h2>The Real Difficulty: LinkedIn Is Not a Stable Document</h2>\n<p>The hard part was not checking whether text includes <code>Reposted</code>.</p>\n<p>The hard part was dealing with LinkedIn as a moving application instead of a static webpage.</p>\n<p>The extension has to survive:</p>\n<ul>\n<li>infinite scroll</li>\n<li>async DOM insertion</li>\n<li>rerendered job cards</li>\n<li>route changes without full page reloads</li>\n<li>different job page variants</li>\n<li>detail panels that do not always expose identity cleanly</li>\n</ul>\n<p>To handle that, the content runtime uses:</p>\n<ul>\n<li>a debounced <code>MutationObserver</code></li>\n<li>scroll-driven rescanning</li>\n<li>route polling</li>\n<li>extension-owned data attributes</li>\n<li>a card registry keyed by job ID</li>\n<li>careful separation between list cards and detail-panel DOM</li>\n</ul>\n<p>Without those pieces, the extension would either miss updates or keep applying the same work again and again.</p>\n<p>That usually leads to flicker, stale markers, duplicate tags, or all the tiny UI disasters that make users uninstall things with impressive speed.</p>\n<h2>Job Identity Is the Backbone</h2>\n<p>Once the extension moved beyond visible DOM detection, job identity became the most important internal primitive.</p>\n<p>If a job can be represented by a stable <code>jobId</code>, then the extension can:</p>\n<ul>\n<li>cache results</li>\n<li>deduplicate background requests</li>\n<li>reapply status after rerenders</li>\n<li>connect detail-panel detection back to the left-side list</li>\n<li>resolve async background results against the correct card</li>\n<li>avoid marking the wrong job</li>\n</ul>\n<p>The extension extracts job IDs from multiple sources because LinkedIn does not always expose them consistently:</p>\n<ul>\n<li><code>/jobs/view/&#x3C;id></code> URL paths</li>\n<li><code>currentJobId</code> query parameters</li>\n<li>DOM attributes</li>\n<li>URN-like fields</li>\n<li>selected-card structures</li>\n</ul>\n<p>This mattered during route investigation, especially when comparing supported <code>/jobs/search/</code> behavior with unsupported route variants where URL shape and selected-card behavior differ.</p>\n<p>The main lesson was simple:</p>\n<blockquote>\n<p>If the identity layer is weak, everything above it becomes guesswork.</p>\n</blockquote>\n<p>And guesswork is not architecture. It is just hope wearing a hoodie.</p>\n<h2>Background Prefetch: Helpful, but Risky</h2>\n<p>Background prefetch is what makes the extension more than a simple highlighter.</p>\n<p>It lets the extension improve the left-side job list over time by fetching unresolved jobs in the background and classifying their reposted status.</p>\n<p>But it also introduces the biggest operational risk:</p>\n<blockquote>\n<p>too many requests.</p>\n</blockquote>\n<p>In practice, that showed up through HTTP <code>429 Too Many Requests</code> responses.</p>\n<p>The fix was not just slowing everything down randomly. The extension needed an actual queue and retry model:</p>\n<ul>\n<li>bounded pending queue</li>\n<li>configurable concurrency</li>\n<li>minimum interval between requests</li>\n<li>pause window after rate limiting</li>\n<li>cooldowns for <code>error</code> and <code>rate_limited</code> states</li>\n<li>cache reuse before new fetches</li>\n<li>viewport-aware candidate selection</li>\n<li>stale-result refresh only when it is worth it</li>\n</ul>\n<p>This changed prefetch from a reckless fire-and-forget feature into a controlled subsystem.</p>\n<p>The real engineering question was not:</p>\n<blockquote>\n<p>“Can we fetch unresolved jobs?”</p>\n</blockquote>\n<p>It was:</p>\n<blockquote>\n<p>“Can we fetch them predictably without annoying LinkedIn, the browser, or the user?”</p>\n</blockquote>\n<p>Small distinction. Large consequences.</p>\n<h2>Viewport Windowing and Queue Control</h2>\n<p>Once prefetch worked, the next problem was deciding what should be prefetched.</p>\n<p>A long LinkedIn jobs page can contain many unresolved cards. Queueing all of them immediately would technically work, but it would be wasteful and fragile.</p>\n<p>Most of those jobs may never enter the user’s attention.</p>\n<p>So the extension uses a viewport-aware model:</p>\n<ul>\n<li>only queue jobs near the current viewport</li>\n<li>score candidates by distance from the viewport</li>\n<li>prioritize closer cards first</li>\n<li>cap the maximum pending queue size</li>\n<li>reuse cached results aggressively</li>\n<li>refresh stale cached results only for nearby jobs</li>\n</ul>\n<p>This makes the extension behave more like a small scheduler than a simple DOM script.</p>\n<p>It is not just asking:</p>\n<blockquote>\n<p>“Which jobs are unresolved?”</p>\n</blockquote>\n<p>It is asking:</p>\n<blockquote>\n<p>“Which unresolved jobs are worth spending background budget on right now?”</p>\n</blockquote>\n<p>That distinction makes the extension much more stable.</p>\n<h2>Route Support: Saying No Improves Reliability</h2>\n<p>One of the better decisions was to make route support explicit.</p>\n<p>The extension does not pretend every LinkedIn Jobs page works the same way. It centralizes route validation in a shared contracts layer and gates unsupported routes instead of silently misbehaving.</p>\n<p>Currently, the main supported route is:</p>\n<ul>\n<li><code>/jobs/search/</code></li>\n</ul>\n<p>The extension intentionally does not support <code>/jobs/search-results/</code> because its selected-card behavior and URL patterns are less reliable for this implementation.</p>\n<p>This is the kind of detail that sounds boring until you debug a browser extension at midnight and realize half your bugs come from pages you never meant to support.</p>\n<p>The better behavior is:</p>\n<ol>\n<li>define supported routes clearly</li>\n<li>enforce the same support contract in content and background code</li>\n<li>show support state in the popup</li>\n<li>fail visibly instead of pretending everything is fine</li>\n</ol>\n<p>That makes bugs easier to reason about and prevents the extension from becoming a pile of lucky assumptions.</p>\n<h2>Popup Controls and Runtime Introspection</h2>\n<p>The popup is not just a settings menu.</p>\n<p>It is also a small operator console.</p>\n<p>It exposes:</p>\n<ul>\n<li>extension enable/disable</li>\n<li>background prefetch toggle</li>\n<li>left-list marking toggle</li>\n<li>detail-panel marking toggle</li>\n<li>prefetch window size</li>\n<li>prefetch concurrency</li>\n<li>cache TTL</li>\n<li>route support state</li>\n<li>queue state</li>\n<li>cache state</li>\n<li>cache clearing</li>\n<li>debug log export</li>\n</ul>\n<p>This helps in two ways.</p>\n<p>First, users can tune how aggressive the extension should be.</p>\n<p>Second, the system becomes diagnosable. That matters because browser extensions have multiple runtime contexts:</p>\n<ul>\n<li>content script</li>\n<li>background worker</li>\n<li>popup</li>\n<li>browser storage</li>\n<li>target page DOM</li>\n</ul>\n<p>When something goes wrong, the reason may live in any of those places, because apparently one runtime context was not enough suffering.</p>\n<h2>Debug Logging Became a Product Feature</h2>\n<p>At first, normal <code>console.log</code> debugging was enough.</p>\n<p>Then the system grew.</p>\n<p>Once prefetching, queue policy, cache state, route support, and popup controls were all interacting, DevTools logs were not enough anymore.</p>\n<p>So the extension added a shared debug log that stores useful runtime events locally and allows exporting them as JSON from the popup.</p>\n<p>The log captures things like:</p>\n<ul>\n<li>settings changes</li>\n<li>content runtime initialization</li>\n<li>route support decisions</li>\n<li>queue enqueue, release, and drop behavior</li>\n<li>fetch completion</li>\n<li>rate-limit events</li>\n<li>status result delivery back to the content runtime</li>\n</ul>\n<p>This sounds like internal tooling, but for a browser extension it becomes part of the product quality.</p>\n<p>If a user says, “This job did not update,” a durable local event trail is much better than asking them to recreate the issue while DevTools is open.</p>\n<p>Nobody wants that. Not even the browser.</p>\n<h2>The Hardest Bugs Were Mapping Bugs</h2>\n<p>The most persistent bugs were not styling bugs.</p>\n<p>They were mapping bugs.</p>\n<p>A common failure looked like this:</p>\n<ol>\n<li>the right-side detail panel correctly showed a reposted signal</li>\n<li>the extension detected it</li>\n<li>the left-side job card should have been updated</li>\n<li>but the extension could not confidently map the detail result back to the correct card</li>\n</ol>\n<p>That led to several hardening steps:</p>\n<ul>\n<li>extract job IDs from multiple sources</li>\n<li>explicitly gate unsupported route families such as <code>search-results</code></li>\n<li>avoid relying on selected-card heuristics for unsupported routes</li>\n<li>fall back to title and company matching when job ID mapping is weak</li>\n<li>avoid accidentally treating detail-panel elements as list cards</li>\n<li>maintain a registry of known cards by job ID</li>\n</ul>\n<p>The lesson here is important:</p>\n<blockquote>\n<p>Finding the signal is only half the problem. Applying it to the right place is the real work.</p>\n</blockquote>\n<p>In UI-heavy browser extensions, synchronization bugs often matter more than detection bugs.</p>\n<h2>Why This Architecture Works</h2>\n<p>The current architecture works because each layer has a clear job:</p>\n<ul>\n<li><strong>DOM detection</strong> handles obvious visible cases quickly</li>\n<li><strong>job identity</strong> gives the system a stable internal key</li>\n<li><strong>card registry</strong> reconnects status to rerendered DOM nodes</li>\n<li><strong>background prefetch</strong> improves coverage</li>\n<li><strong>cache policy</strong> avoids repeated work</li>\n<li><strong>queue policy</strong> prevents request spikes</li>\n<li><strong>route support</strong> keeps behavior honest</li>\n<li><strong>popup controls</strong> make the system tunable</li>\n<li><strong>debug logs</strong> make the system observable</li>\n</ul>\n<p>None of these pieces are individually fancy.</p>\n<p>The value comes from how they work together under real browser-extension constraints:</p>\n<ul>\n<li>unstable target DOM</li>\n<li>asynchronous UI updates</li>\n<li>multiple runtime contexts</li>\n<li>background request limits</li>\n<li>user-facing latency expectations</li>\n<li>page behavior the extension does not control</li>\n</ul>\n<p>That is what made the project interesting.</p>\n<p>Not the word matching. Not the red styling. The actual work was making the system keep doing the right thing while LinkedIn kept changing underneath it.</p>\n<h2>Lessons for Other Browser Extensions</h2>\n<p>Here are the lessons I would reuse in future browser-extension projects.</p>\n<h3>1. Start with the user-visible core</h3>\n<p>Build the simplest thing that creates real user value.</p>\n<p>For this project, that was marking visible reposted jobs. Everything else came after that.</p>\n<h3>2. Treat identity as infrastructure</h3>\n<p>If async work needs to reconnect back to the page later, stable identity is not optional.</p>\n<p>A weak identity model will leak bugs everywhere.</p>\n<h3>3. Do not let background work run wild</h3>\n<p>Background fetching needs rules:</p>\n<ul>\n<li>queue limits</li>\n<li>cache reuse</li>\n<li>retry policy</li>\n<li>rate-limit handling</li>\n<li>prioritization</li>\n</ul>\n<p>Otherwise, it becomes chaos with a network tab.</p>\n<h3>4. Be honest about support boundaries</h3>\n<p>A route support matrix is better than pretending every page variant works.</p>\n<p>Partial support should be explicit.</p>\n<h3>5. Build observability early</h3>\n<p>Once an extension spans content scripts, background workers, popup UI, storage, and a dynamic target page, debugging without logs becomes painful fast.</p>\n<p>Small projects still deserve good diagnostics.</p>\n<h2>Closing Thoughts</h2>\n<p>LinkedIn Reposted Marker started as a small utility.</p>\n<p>The original idea was just:</p>\n<blockquote>\n<p>highlight reposted jobs so users can avoid wasting time.</p>\n</blockquote>\n<p>But making that simple feature reliable required a real architecture:</p>\n<ul>\n<li>DOM scanning</li>\n<li>identity extraction</li>\n<li>card mapping</li>\n<li>background prefetch</li>\n<li>cache policy</li>\n<li>queue control</li>\n<li>route gating</li>\n<li>popup controls</li>\n<li>debug logs</li>\n</ul>\n<p>That is the real lesson from this project.</p>\n<p>Browser extensions are not just little scripts you inject into a page. At least, not if you want them to survive real-world usage.</p>\n<p>They are small distributed systems running inside a browser, attached to someone else’s constantly changing application, trying to be helpful without breaking things.</p>\n<p>Which is ridiculous.</p>\n<p>But also kind of fun.</p>"
  },
  {
    "slug": "gpu-cluster-storage-design",
    "title": "Storage Design for a Hybrid Login + Compute Node",
    "date": "2026-04-21",
    "summary": "Trade-offs behind using ext4, XFS, LVM, and project quotas for a hybrid login and compute node with room to grow into a cluster.",
    "tags": [
      "Infrastructure",
      "Storage",
      "GPU Cluster"
    ],
    "html": "<h2>Overview</h2>\n<p>This document captures the reasoning, design decisions, and implementation details for setting up storage on a GPU server that initially serves as both a <strong>login node</strong> and a <strong>compute node</strong>, with future expansion into a multi-node cluster.</p>\n<p>Instead of following a rigid \"textbook\" layout, we intentionally designed a system that balances:</p>\n<ul>\n<li>Performance</li>\n<li>Flexibility</li>\n<li>Operational simplicity</li>\n<li>Future scalability</li>\n</ul>\n<hr>\n<h1>Key Concepts</h1>\n<h2>LVM (Logical Volume Manager)</h2>\n<p>LVM provides a flexible abstraction layer over physical storage:</p>\n<pre><code class=\"hljs\">Physical Disk → PV → VG → LV\n</code></pre>\n<p>It allows:</p>\n<ul>\n<li>Pooling storage into a single volume group</li>\n<li>Dynamically allocating logical volumes</li>\n<li>Future expansion by adding disks</li>\n</ul>\n<p>However:</p>\n<blockquote>\n<p>LVM does not eliminate filesystem constraints (e.g., XFS cannot shrink)</p>\n</blockquote>\n<hr>\n<h2>ext4</h2>\n<p>A general-purpose filesystem focused on:</p>\n<ul>\n<li>Stability</li>\n<li>Simplicity</li>\n<li>Mature recovery tooling</li>\n</ul>\n<p>Best suited for:</p>\n<ul>\n<li>System partitions</li>\n<li>Conservative environments</li>\n</ul>\n<hr>\n<h2>XFS</h2>\n<p>A high-performance filesystem designed for:</p>\n<ul>\n<li>Large files</li>\n<li>High throughput</li>\n<li>Parallel I/O workloads</li>\n</ul>\n<p>Key characteristics:</p>\n<ul>\n<li>Excellent for data-intensive systems</li>\n<li>Widely used in production clusters</li>\n<li>Supports project quota (pquota)</li>\n</ul>\n<p>Limitations:</p>\n<ul>\n<li>Cannot shrink volumes</li>\n<li>Requires more deliberate planning</li>\n</ul>\n<hr>\n<h2>Project Quota (pquota)</h2>\n<p>XFS provides <strong>project-based quota</strong>, which allows limiting disk usage at the directory level.</p>\n<p>Unlike traditional user/group quota:</p>\n<ul>\n<li>Applies to directory trees</li>\n<li>Suitable for workload isolation (e.g., <code>/data</code> vs <code>/scratch</code>)</li>\n</ul>\n<hr>\n<h1>Problem Context</h1>\n<p>We are working with:</p>\n<ul>\n<li>~2TB local NVMe storage</li>\n<li>200GB already allocated for system (<code>/</code>)</li>\n<li>~1.8TB remaining</li>\n</ul>\n<p>Node role:</p>\n<ul>\n<li>Login node (interactive usage)</li>\n<li>Compute node (jobs, experiments)</li>\n</ul>\n<p>Future direction:</p>\n<ul>\n<li>Expand into multi-node cluster</li>\n<li>Add storage via SSD or NAS</li>\n<li>Avoid reliance on resizing/shrinking partitions</li>\n</ul>\n<hr>\n<h1>Design Options Considered</h1>\n<h2>Option 1: Hard Partition / LVM Split</h2>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate logical volumes for:</p>\n<ul>\n<li>Persistent data</li>\n<li>Temporary data</li>\n</ul>\n<h3>Pros</h3>\n<ul>\n<li>Strong isolation</li>\n<li>Easy observability (<code>df -h</code>)</li>\n<li>Clear mental model</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>Requires early capacity decisions</li>\n<li>XFS cannot shrink → misallocation is costly</li>\n<li>Less flexible during early-stage usage</li>\n</ul>\n<hr>\n<h2>Option 2: Directory-based Separation Only</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/scratch\n</code></pre>\n<h3>Pros</h3>\n<ul>\n<li>Simple</li>\n<li>No upfront partitioning decisions</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>No isolation</li>\n<li>High risk of disk exhaustion</li>\n<li>Not suitable for multi-user environments</li>\n</ul>\n<hr>\n<h2>Option 3: Single XFS + Project Quota (<strong>Chosen</strong>)</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/persistent\n/data/scratch\n</code></pre>\n<p>Use:</p>\n<ul>\n<li>XFS as the underlying filesystem</li>\n<li>Project quota (pquota) to enforce limits</li>\n</ul>\n<hr>\n<h1>Decision Trade-offs</h1>\n<p>This decision is fundamentally a trade-off between:</p>\n<h3>1. Isolation vs Flexibility</h3>\n<table>\n<thead>\n<tr>\n<th>Approach</th>\n<th>Isolation</th>\n<th>Flexibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Partition split</td>\n<td>Strong</td>\n<td>Low</td>\n</tr>\n<tr>\n<td>XFS + pquota</td>\n<td>Medium</td>\n<td>High</td>\n</tr>\n</tbody>\n</table>\n<p>We chose <strong>flexibility</strong> because:</p>\n<ul>\n<li>Early-stage workload is uncertain</li>\n<li>Data vs scratch usage ratio may evolve</li>\n<li>Repartitioning later is costly</li>\n</ul>\n<hr>\n<h3>2. Operational Safety vs Adaptability</h3>\n<p>Partition-based design:</p>\n<ul>\n<li>Safer by default</li>\n<li>Enforced by the OS</li>\n</ul>\n<p>Quota-based design:</p>\n<ul>\n<li>Requires correct configuration</li>\n<li>Depends on operational discipline</li>\n</ul>\n<p>We accepted this trade-off because:</p>\n<blockquote>\n<p>Our system is still evolving, and adaptability is more valuable than strict enforcement at this stage.</p>\n</blockquote>\n<hr>\n<h3>3. Observability vs Control</h3>\n<p>Partition split:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">df</span> -h\n</code></pre>\n<p>→ Immediate visibility</p>\n<p>Quota-based:</p>\n<pre><code class=\"hljs language-arduino\">xfs_quota -x -c <span class=\"hljs-string\">\"report\"</span>\n</code></pre>\n<p>→ Requires tooling</p>\n<p>We accept slightly reduced observability in exchange for:</p>\n<ul>\n<li>Dynamic capacity management</li>\n<li>Avoiding premature optimization</li>\n</ul>\n<hr>\n<h3>4. Production Convention vs Practical Engineering</h3>\n<p>Typical production clusters use:</p>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate mount points.</p>\n<p>We intentionally diverge because:</p>\n<ul>\n<li>This is an early-stage system</li>\n<li>We prioritize iteration speed over convention</li>\n<li>We plan to evolve toward stricter isolation later if needed</li>\n</ul>\n<hr>\n<h1>Decision Rationale</h1>\n<p>We chose <strong>XFS + project quota</strong> based on the following reasoning:</p>\n<hr>\n<h2>1. Workload is large-file and throughput-heavy</h2>\n<p>The system handles:</p>\n<ul>\n<li>Models (GB–TB scale)</li>\n<li>Datasets</li>\n<li>Checkpoints</li>\n<li>Container layers</li>\n</ul>\n<p>XFS is well-suited for:</p>\n<ul>\n<li>Sequential I/O</li>\n<li>Large file handling</li>\n<li>High throughput operations</li>\n</ul>\n<hr>\n<h2>2. Need for dynamic capacity allocation</h2>\n<p>We do not yet know:</p>\n<ul>\n<li>Exact ratio of persistent vs temporary data</li>\n<li>Future workload patterns</li>\n</ul>\n<p>Using pquota allows:</p>\n<ul>\n<li>Adjusting limits without repartitioning</li>\n<li>Avoiding irreversible decisions</li>\n</ul>\n<hr>\n<h2>3. Scaling strategy does not depend on shrinking</h2>\n<p>We explicitly decided:</p>\n<ul>\n<li>\n<p>No reliance on shrinking volumes</p>\n</li>\n<li>\n<p>Scaling will be done by:</p>\n<ul>\n<li>Adding SSDs</li>\n<li>Integrating NAS</li>\n</ul>\n</li>\n</ul>\n<p>This removes a key disadvantage of XFS.</p>\n<hr>\n<h2>4. Early-stage system prioritizes iteration</h2>\n<p>At this stage:</p>\n<ul>\n<li>Requirements are evolving</li>\n<li>Usage patterns are not fully stable</li>\n</ul>\n<p>We optimize for:</p>\n<blockquote>\n<p>Speed of iteration over rigid correctness</p>\n</blockquote>\n<hr>\n<h2>5. Acceptable level of risk</h2>\n<p>We acknowledge:</p>\n<ul>\n<li>Quota is not as strong as partition isolation</li>\n<li>Misconfiguration is possible</li>\n</ul>\n<p>But we mitigate this by:</p>\n<ul>\n<li>Clear directory structure</li>\n<li>Explicit usage guidelines</li>\n<li>Monitoring and documentation</li>\n</ul>\n<hr>\n<h1>Final Layout</h1>\n<pre><code class=\"hljs language-bash\">/            → ext4 (system)\n/data        → XFS\n  /persistent\n  /scratch\n</code></pre>\n<p>Quota is enforced via:</p>\n<ul>\n<li>XFS project quota</li>\n<li>Separate limits for persistent and scratch</li>\n</ul>\n<hr>\n<h1>Operational Guidelines</h1>\n<h2>Directory Usage</h2>\n<h3>Persistent Data</h3>\n<pre><code class=\"hljs language-bash\">/data/persistent\n</code></pre>\n<ul>\n<li>models</li>\n<li>datasets</li>\n<li>repositories</li>\n<li>shared artifacts</li>\n<li>Docker root</li>\n</ul>\n<hr>\n<h3>Temporary Data</h3>\n<pre><code class=\"hljs language-bash\">/data/scratch\n</code></pre>\n<ul>\n<li>checkpoints</li>\n<li>cache</li>\n<li>build artifacts</li>\n<li>intermediate outputs</li>\n</ul>\n<hr>\n<h2>Slurm Example</h2>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">export</span> TMPDIR=/data/scratch/<span class=\"hljs-variable\">$USER</span>/<span class=\"hljs-variable\">$SLURM_JOB_ID</span>\n<span class=\"hljs-built_in\">mkdir</span> -p <span class=\"hljs-variable\">$TMPDIR</span>\n</code></pre>\n<hr>\n<h2>Docker</h2>\n<p>Docker root should be moved to:</p>\n<pre><code class=\"hljs language-bash\">/data/persistent/docker\n</code></pre>\n<hr>\n<h1>Future Evolution</h1>\n<p>This design is not final.</p>\n<p>Possible future changes:</p>\n<ul>\n<li>Split <code>/data</code> and <code>/scratch</code> into separate volumes</li>\n<li>Introduce network storage (NAS / distributed FS)</li>\n<li>Move heavy workloads to dedicated compute nodes</li>\n</ul>\n<hr>\n<h1>Lessons Learned</h1>\n<ol>\n<li>Storage design is about trade-offs, not absolutes</li>\n<li>Filesystem choice must match workload characteristics</li>\n<li>Early-stage systems benefit from flexibility</li>\n<li>Isolation can be introduced later, but rigidity is hard to undo</li>\n<li>Production design evolves — it is not fixed from day one</li>\n</ol>\n<hr>\n<h1>Conclusion</h1>\n<p>We chose <strong>XFS + project quota</strong> because it provides:</p>\n<ul>\n<li>High performance for data-heavy workloads</li>\n<li>Flexibility during early-stage development</li>\n<li>A balance between structure and adaptability</li>\n</ul>\n<blockquote>\n<p>Instead of optimizing for a perfect final state, we optimized for a system that can evolve.</p>\n</blockquote>"
  }
];

export const blogPostBySlug: Record<string, BlogPost> = {
  "linkedin-reposted-marker": {
    "slug": "linkedin-reposted-marker",
    "title": "Building a LinkedIn Reposted Marker Extension",
    "date": "2026-04-23",
    "summary": "Detect reposted jobs, prefetch unresolved listings, and keep LinkedIn highlights stable across route changes and rerenders.",
    "tags": [
      "Browser Extension",
      "LinkedIn"
    ],
    "html": "<p><a href=\"https://github.com/pinkpig777/reposted-marker\" target=\"_blank\" rel=\"noopener noreferrer\">GitHub Link</a></p>\n<h2>Introduction</h2>\n<p>Most browser extensions look simple from the outside.</p>\n<p>Then you open the codebase and realize the browser is basically a haunted house with event listeners.</p>\n<p><strong>LinkedIn Reposted Marker</strong> started with a simple goal: help users identify reposted LinkedIn jobs directly from the jobs list, before they waste time clicking into each post.</p>\n<p>The idea is straightforward:</p>\n<blockquote>\n<p>If a job is reposted, show that signal earlier.</p>\n</blockquote>\n<p>But building that reliably was not just a matter of finding the word <code>Reposted</code>.</p>\n<p>LinkedIn is a dynamic single-page app. The page changes while you scroll, cards rerender, routes change without full reloads, and useful information sometimes appears only in the detail panel after a job is opened.</p>\n<p>So this small extension slowly turned into a real browser-extension engineering problem:</p>\n<ul>\n<li>detect reposted jobs from visible DOM when possible</li>\n<li>connect detail-panel signals back to the correct list card</li>\n<li>prefetch unresolved jobs in the background</li>\n<li>avoid sending too many requests</li>\n<li>keep highlights stable across scrolls, route changes, and rerenders</li>\n<li>give the user enough controls and diagnostics to understand what is happening</li>\n</ul>\n<p>This post walks through how the extension is designed, why the architecture ended up this way, and what I learned from making it reliable enough to use.</p>\n<h2>The Product Problem</h2>\n<p>The core user problem is simple:</p>\n<blockquote>\n<p>“Can I know this job is reposted before I spend attention opening it?”</p>\n</blockquote>\n<p>LinkedIn may show the reposted signal inside the job detail panel, but by that point the user has already clicked.</p>\n<p>That is too late.</p>\n<p>A basic extension could highlight reposted text if it is already visible in the left-side job list. That is useful, but limited. A better version needs to do more:</p>\n<ol>\n<li>detect reposted status from visible job cards</li>\n<li>detect status from the opened job detail panel</li>\n<li>map the detail-panel result back to the correct left-side card</li>\n<li>prefetch unresolved jobs in the background</li>\n<li>keep the UI stable while LinkedIn constantly mutates the page, because apparently static HTML was too peaceful for humanity</li>\n</ol>\n<p>That product need shaped the whole architecture.</p>\n<h2>High-Level Architecture</h2>\n<p>The extension has four main runtime pieces:</p>\n<ol>\n<li>\n<p><strong>Content runtime</strong></p>\n<p>This runs directly on supported LinkedIn jobs pages. It scans job cards, extracts job IDs, detects visible reposted signals, and applies visual markers to both the left-side list and the right-side detail panel.</p>\n</li>\n<li>\n<p><strong>Background worker</strong></p>\n<p>This handles async prefetching. When the content script finds unresolved jobs, it can ask the background worker to fetch and classify those jobs without blocking the page.</p>\n</li>\n<li>\n<p><strong>Shared policy layer</strong></p>\n<p>This centralizes route support, payload validation, cache rules, timing constants, and debug behavior. Without this layer, content and background logic would slowly drift apart, because duplicated logic always finds a way to embarrass you later.</p>\n</li>\n<li>\n<p><strong>Popup control menu</strong></p>\n<p>This gives the user runtime controls and diagnostics: enable/disable behavior, prefetch tuning, cache TTL, queue state, cache clearing, and debug log export.</p>\n</li>\n</ol>\n<p>The runtime flow looks like this:</p>\n<div class=\"mermaid\">flowchart LR\n  A[\"LinkedIn Jobs Page\"] --> B[\"Extension Content Runtime\"]\n  B --> C[\"Visible DOM Detection\"]\n  B --> D[\"Background Prefetch for Unresolved Jobs\"]\n\n  D --> E[\"Background Worker\"]\n  E --> F[\"Fetch and Classify Job Status\"]\n  E --> G[\"Cache Results\"]\n\n  G --> H[\"Return Results to Content Runtime\"]\n  H --> I[\"Highlight List Cards and Detail Panel\"]\n\n  J[\"Popup Control Menu\"] --> K[\"Settings and Diagnostics\"]\n  K --> B\n  K --> E\n  K --> G\n</div>\n<p>The important design decision is this:</p>\n<blockquote>\n<p>Background prefetch should improve DOM detection, not replace it.</p>\n</blockquote>\n<p>That means the extension still works even if prefetch is disabled, paused, rate-limited, or temporarily unavailable.</p>\n<h2>Why I Started with DOM Detection</h2>\n<p>The most tempting path was to start with background fetching first.</p>\n<p>That would have been the fun, over-engineered version. Naturally, also the wrong starting point.</p>\n<p>The right starting point was visible DOM detection.</p>\n<p>There were three reasons.</p>\n<p>First, it validated the product quickly. If marking reposted jobs in the list was not actually useful, there was no point building a bigger system around it.</p>\n<p>Second, it forced me to solve the basic page-mapping problem. Before doing async work, the extension needed to understand what a job card is, how to find it, how to extract its identity, and how to update it later.</p>\n<p>Third, DOM detection gave the extension a fallback path. Background prefetch depends on network behavior, route support, cache state, and rate limits. DOM detection is cheaper, faster, and less fragile.</p>\n<p>So the project evolved in layers:</p>\n<ol>\n<li>visible detection</li>\n<li>stable job identity</li>\n<li>cache and registry</li>\n<li>background prefetch</li>\n<li>queue control</li>\n<li>popup controls and debug logging</li>\n</ol>\n<p>That order mattered.</p>\n<h2>The Real Difficulty: LinkedIn Is Not a Stable Document</h2>\n<p>The hard part was not checking whether text includes <code>Reposted</code>.</p>\n<p>The hard part was dealing with LinkedIn as a moving application instead of a static webpage.</p>\n<p>The extension has to survive:</p>\n<ul>\n<li>infinite scroll</li>\n<li>async DOM insertion</li>\n<li>rerendered job cards</li>\n<li>route changes without full page reloads</li>\n<li>different job page variants</li>\n<li>detail panels that do not always expose identity cleanly</li>\n</ul>\n<p>To handle that, the content runtime uses:</p>\n<ul>\n<li>a debounced <code>MutationObserver</code></li>\n<li>scroll-driven rescanning</li>\n<li>route polling</li>\n<li>extension-owned data attributes</li>\n<li>a card registry keyed by job ID</li>\n<li>careful separation between list cards and detail-panel DOM</li>\n</ul>\n<p>Without those pieces, the extension would either miss updates or keep applying the same work again and again.</p>\n<p>That usually leads to flicker, stale markers, duplicate tags, or all the tiny UI disasters that make users uninstall things with impressive speed.</p>\n<h2>Job Identity Is the Backbone</h2>\n<p>Once the extension moved beyond visible DOM detection, job identity became the most important internal primitive.</p>\n<p>If a job can be represented by a stable <code>jobId</code>, then the extension can:</p>\n<ul>\n<li>cache results</li>\n<li>deduplicate background requests</li>\n<li>reapply status after rerenders</li>\n<li>connect detail-panel detection back to the left-side list</li>\n<li>resolve async background results against the correct card</li>\n<li>avoid marking the wrong job</li>\n</ul>\n<p>The extension extracts job IDs from multiple sources because LinkedIn does not always expose them consistently:</p>\n<ul>\n<li><code>/jobs/view/&#x3C;id></code> URL paths</li>\n<li><code>currentJobId</code> query parameters</li>\n<li>DOM attributes</li>\n<li>URN-like fields</li>\n<li>selected-card structures</li>\n</ul>\n<p>This mattered during route investigation, especially when comparing supported <code>/jobs/search/</code> behavior with unsupported route variants where URL shape and selected-card behavior differ.</p>\n<p>The main lesson was simple:</p>\n<blockquote>\n<p>If the identity layer is weak, everything above it becomes guesswork.</p>\n</blockquote>\n<p>And guesswork is not architecture. It is just hope wearing a hoodie.</p>\n<h2>Background Prefetch: Helpful, but Risky</h2>\n<p>Background prefetch is what makes the extension more than a simple highlighter.</p>\n<p>It lets the extension improve the left-side job list over time by fetching unresolved jobs in the background and classifying their reposted status.</p>\n<p>But it also introduces the biggest operational risk:</p>\n<blockquote>\n<p>too many requests.</p>\n</blockquote>\n<p>In practice, that showed up through HTTP <code>429 Too Many Requests</code> responses.</p>\n<p>The fix was not just slowing everything down randomly. The extension needed an actual queue and retry model:</p>\n<ul>\n<li>bounded pending queue</li>\n<li>configurable concurrency</li>\n<li>minimum interval between requests</li>\n<li>pause window after rate limiting</li>\n<li>cooldowns for <code>error</code> and <code>rate_limited</code> states</li>\n<li>cache reuse before new fetches</li>\n<li>viewport-aware candidate selection</li>\n<li>stale-result refresh only when it is worth it</li>\n</ul>\n<p>This changed prefetch from a reckless fire-and-forget feature into a controlled subsystem.</p>\n<p>The real engineering question was not:</p>\n<blockquote>\n<p>“Can we fetch unresolved jobs?”</p>\n</blockquote>\n<p>It was:</p>\n<blockquote>\n<p>“Can we fetch them predictably without annoying LinkedIn, the browser, or the user?”</p>\n</blockquote>\n<p>Small distinction. Large consequences.</p>\n<h2>Viewport Windowing and Queue Control</h2>\n<p>Once prefetch worked, the next problem was deciding what should be prefetched.</p>\n<p>A long LinkedIn jobs page can contain many unresolved cards. Queueing all of them immediately would technically work, but it would be wasteful and fragile.</p>\n<p>Most of those jobs may never enter the user’s attention.</p>\n<p>So the extension uses a viewport-aware model:</p>\n<ul>\n<li>only queue jobs near the current viewport</li>\n<li>score candidates by distance from the viewport</li>\n<li>prioritize closer cards first</li>\n<li>cap the maximum pending queue size</li>\n<li>reuse cached results aggressively</li>\n<li>refresh stale cached results only for nearby jobs</li>\n</ul>\n<p>This makes the extension behave more like a small scheduler than a simple DOM script.</p>\n<p>It is not just asking:</p>\n<blockquote>\n<p>“Which jobs are unresolved?”</p>\n</blockquote>\n<p>It is asking:</p>\n<blockquote>\n<p>“Which unresolved jobs are worth spending background budget on right now?”</p>\n</blockquote>\n<p>That distinction makes the extension much more stable.</p>\n<h2>Route Support: Saying No Improves Reliability</h2>\n<p>One of the better decisions was to make route support explicit.</p>\n<p>The extension does not pretend every LinkedIn Jobs page works the same way. It centralizes route validation in a shared contracts layer and gates unsupported routes instead of silently misbehaving.</p>\n<p>Currently, the main supported route is:</p>\n<ul>\n<li><code>/jobs/search/</code></li>\n</ul>\n<p>The extension intentionally does not support <code>/jobs/search-results/</code> because its selected-card behavior and URL patterns are less reliable for this implementation.</p>\n<p>This is the kind of detail that sounds boring until you debug a browser extension at midnight and realize half your bugs come from pages you never meant to support.</p>\n<p>The better behavior is:</p>\n<ol>\n<li>define supported routes clearly</li>\n<li>enforce the same support contract in content and background code</li>\n<li>show support state in the popup</li>\n<li>fail visibly instead of pretending everything is fine</li>\n</ol>\n<p>That makes bugs easier to reason about and prevents the extension from becoming a pile of lucky assumptions.</p>\n<h2>Popup Controls and Runtime Introspection</h2>\n<p>The popup is not just a settings menu.</p>\n<p>It is also a small operator console.</p>\n<p>It exposes:</p>\n<ul>\n<li>extension enable/disable</li>\n<li>background prefetch toggle</li>\n<li>left-list marking toggle</li>\n<li>detail-panel marking toggle</li>\n<li>prefetch window size</li>\n<li>prefetch concurrency</li>\n<li>cache TTL</li>\n<li>route support state</li>\n<li>queue state</li>\n<li>cache state</li>\n<li>cache clearing</li>\n<li>debug log export</li>\n</ul>\n<p>This helps in two ways.</p>\n<p>First, users can tune how aggressive the extension should be.</p>\n<p>Second, the system becomes diagnosable. That matters because browser extensions have multiple runtime contexts:</p>\n<ul>\n<li>content script</li>\n<li>background worker</li>\n<li>popup</li>\n<li>browser storage</li>\n<li>target page DOM</li>\n</ul>\n<p>When something goes wrong, the reason may live in any of those places, because apparently one runtime context was not enough suffering.</p>\n<h2>Debug Logging Became a Product Feature</h2>\n<p>At first, normal <code>console.log</code> debugging was enough.</p>\n<p>Then the system grew.</p>\n<p>Once prefetching, queue policy, cache state, route support, and popup controls were all interacting, DevTools logs were not enough anymore.</p>\n<p>So the extension added a shared debug log that stores useful runtime events locally and allows exporting them as JSON from the popup.</p>\n<p>The log captures things like:</p>\n<ul>\n<li>settings changes</li>\n<li>content runtime initialization</li>\n<li>route support decisions</li>\n<li>queue enqueue, release, and drop behavior</li>\n<li>fetch completion</li>\n<li>rate-limit events</li>\n<li>status result delivery back to the content runtime</li>\n</ul>\n<p>This sounds like internal tooling, but for a browser extension it becomes part of the product quality.</p>\n<p>If a user says, “This job did not update,” a durable local event trail is much better than asking them to recreate the issue while DevTools is open.</p>\n<p>Nobody wants that. Not even the browser.</p>\n<h2>The Hardest Bugs Were Mapping Bugs</h2>\n<p>The most persistent bugs were not styling bugs.</p>\n<p>They were mapping bugs.</p>\n<p>A common failure looked like this:</p>\n<ol>\n<li>the right-side detail panel correctly showed a reposted signal</li>\n<li>the extension detected it</li>\n<li>the left-side job card should have been updated</li>\n<li>but the extension could not confidently map the detail result back to the correct card</li>\n</ol>\n<p>That led to several hardening steps:</p>\n<ul>\n<li>extract job IDs from multiple sources</li>\n<li>explicitly gate unsupported route families such as <code>search-results</code></li>\n<li>avoid relying on selected-card heuristics for unsupported routes</li>\n<li>fall back to title and company matching when job ID mapping is weak</li>\n<li>avoid accidentally treating detail-panel elements as list cards</li>\n<li>maintain a registry of known cards by job ID</li>\n</ul>\n<p>The lesson here is important:</p>\n<blockquote>\n<p>Finding the signal is only half the problem. Applying it to the right place is the real work.</p>\n</blockquote>\n<p>In UI-heavy browser extensions, synchronization bugs often matter more than detection bugs.</p>\n<h2>Why This Architecture Works</h2>\n<p>The current architecture works because each layer has a clear job:</p>\n<ul>\n<li><strong>DOM detection</strong> handles obvious visible cases quickly</li>\n<li><strong>job identity</strong> gives the system a stable internal key</li>\n<li><strong>card registry</strong> reconnects status to rerendered DOM nodes</li>\n<li><strong>background prefetch</strong> improves coverage</li>\n<li><strong>cache policy</strong> avoids repeated work</li>\n<li><strong>queue policy</strong> prevents request spikes</li>\n<li><strong>route support</strong> keeps behavior honest</li>\n<li><strong>popup controls</strong> make the system tunable</li>\n<li><strong>debug logs</strong> make the system observable</li>\n</ul>\n<p>None of these pieces are individually fancy.</p>\n<p>The value comes from how they work together under real browser-extension constraints:</p>\n<ul>\n<li>unstable target DOM</li>\n<li>asynchronous UI updates</li>\n<li>multiple runtime contexts</li>\n<li>background request limits</li>\n<li>user-facing latency expectations</li>\n<li>page behavior the extension does not control</li>\n</ul>\n<p>That is what made the project interesting.</p>\n<p>Not the word matching. Not the red styling. The actual work was making the system keep doing the right thing while LinkedIn kept changing underneath it.</p>\n<h2>Lessons for Other Browser Extensions</h2>\n<p>Here are the lessons I would reuse in future browser-extension projects.</p>\n<h3>1. Start with the user-visible core</h3>\n<p>Build the simplest thing that creates real user value.</p>\n<p>For this project, that was marking visible reposted jobs. Everything else came after that.</p>\n<h3>2. Treat identity as infrastructure</h3>\n<p>If async work needs to reconnect back to the page later, stable identity is not optional.</p>\n<p>A weak identity model will leak bugs everywhere.</p>\n<h3>3. Do not let background work run wild</h3>\n<p>Background fetching needs rules:</p>\n<ul>\n<li>queue limits</li>\n<li>cache reuse</li>\n<li>retry policy</li>\n<li>rate-limit handling</li>\n<li>prioritization</li>\n</ul>\n<p>Otherwise, it becomes chaos with a network tab.</p>\n<h3>4. Be honest about support boundaries</h3>\n<p>A route support matrix is better than pretending every page variant works.</p>\n<p>Partial support should be explicit.</p>\n<h3>5. Build observability early</h3>\n<p>Once an extension spans content scripts, background workers, popup UI, storage, and a dynamic target page, debugging without logs becomes painful fast.</p>\n<p>Small projects still deserve good diagnostics.</p>\n<h2>Closing Thoughts</h2>\n<p>LinkedIn Reposted Marker started as a small utility.</p>\n<p>The original idea was just:</p>\n<blockquote>\n<p>highlight reposted jobs so users can avoid wasting time.</p>\n</blockquote>\n<p>But making that simple feature reliable required a real architecture:</p>\n<ul>\n<li>DOM scanning</li>\n<li>identity extraction</li>\n<li>card mapping</li>\n<li>background prefetch</li>\n<li>cache policy</li>\n<li>queue control</li>\n<li>route gating</li>\n<li>popup controls</li>\n<li>debug logs</li>\n</ul>\n<p>That is the real lesson from this project.</p>\n<p>Browser extensions are not just little scripts you inject into a page. At least, not if you want them to survive real-world usage.</p>\n<p>They are small distributed systems running inside a browser, attached to someone else’s constantly changing application, trying to be helpful without breaking things.</p>\n<p>Which is ridiculous.</p>\n<p>But also kind of fun.</p>"
  },
  "gpu-cluster-storage-design": {
    "slug": "gpu-cluster-storage-design",
    "title": "Storage Design for a Hybrid Login + Compute Node",
    "date": "2026-04-21",
    "summary": "Trade-offs behind using ext4, XFS, LVM, and project quotas for a hybrid login and compute node with room to grow into a cluster.",
    "tags": [
      "Infrastructure",
      "Storage",
      "GPU Cluster"
    ],
    "html": "<h2>Overview</h2>\n<p>This document captures the reasoning, design decisions, and implementation details for setting up storage on a GPU server that initially serves as both a <strong>login node</strong> and a <strong>compute node</strong>, with future expansion into a multi-node cluster.</p>\n<p>Instead of following a rigid \"textbook\" layout, we intentionally designed a system that balances:</p>\n<ul>\n<li>Performance</li>\n<li>Flexibility</li>\n<li>Operational simplicity</li>\n<li>Future scalability</li>\n</ul>\n<hr>\n<h1>Key Concepts</h1>\n<h2>LVM (Logical Volume Manager)</h2>\n<p>LVM provides a flexible abstraction layer over physical storage:</p>\n<pre><code class=\"hljs\">Physical Disk → PV → VG → LV\n</code></pre>\n<p>It allows:</p>\n<ul>\n<li>Pooling storage into a single volume group</li>\n<li>Dynamically allocating logical volumes</li>\n<li>Future expansion by adding disks</li>\n</ul>\n<p>However:</p>\n<blockquote>\n<p>LVM does not eliminate filesystem constraints (e.g., XFS cannot shrink)</p>\n</blockquote>\n<hr>\n<h2>ext4</h2>\n<p>A general-purpose filesystem focused on:</p>\n<ul>\n<li>Stability</li>\n<li>Simplicity</li>\n<li>Mature recovery tooling</li>\n</ul>\n<p>Best suited for:</p>\n<ul>\n<li>System partitions</li>\n<li>Conservative environments</li>\n</ul>\n<hr>\n<h2>XFS</h2>\n<p>A high-performance filesystem designed for:</p>\n<ul>\n<li>Large files</li>\n<li>High throughput</li>\n<li>Parallel I/O workloads</li>\n</ul>\n<p>Key characteristics:</p>\n<ul>\n<li>Excellent for data-intensive systems</li>\n<li>Widely used in production clusters</li>\n<li>Supports project quota (pquota)</li>\n</ul>\n<p>Limitations:</p>\n<ul>\n<li>Cannot shrink volumes</li>\n<li>Requires more deliberate planning</li>\n</ul>\n<hr>\n<h2>Project Quota (pquota)</h2>\n<p>XFS provides <strong>project-based quota</strong>, which allows limiting disk usage at the directory level.</p>\n<p>Unlike traditional user/group quota:</p>\n<ul>\n<li>Applies to directory trees</li>\n<li>Suitable for workload isolation (e.g., <code>/data</code> vs <code>/scratch</code>)</li>\n</ul>\n<hr>\n<h1>Problem Context</h1>\n<p>We are working with:</p>\n<ul>\n<li>~2TB local NVMe storage</li>\n<li>200GB already allocated for system (<code>/</code>)</li>\n<li>~1.8TB remaining</li>\n</ul>\n<p>Node role:</p>\n<ul>\n<li>Login node (interactive usage)</li>\n<li>Compute node (jobs, experiments)</li>\n</ul>\n<p>Future direction:</p>\n<ul>\n<li>Expand into multi-node cluster</li>\n<li>Add storage via SSD or NAS</li>\n<li>Avoid reliance on resizing/shrinking partitions</li>\n</ul>\n<hr>\n<h1>Design Options Considered</h1>\n<h2>Option 1: Hard Partition / LVM Split</h2>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate logical volumes for:</p>\n<ul>\n<li>Persistent data</li>\n<li>Temporary data</li>\n</ul>\n<h3>Pros</h3>\n<ul>\n<li>Strong isolation</li>\n<li>Easy observability (<code>df -h</code>)</li>\n<li>Clear mental model</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>Requires early capacity decisions</li>\n<li>XFS cannot shrink → misallocation is costly</li>\n<li>Less flexible during early-stage usage</li>\n</ul>\n<hr>\n<h2>Option 2: Directory-based Separation Only</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/scratch\n</code></pre>\n<h3>Pros</h3>\n<ul>\n<li>Simple</li>\n<li>No upfront partitioning decisions</li>\n</ul>\n<h3>Cons</h3>\n<ul>\n<li>No isolation</li>\n<li>High risk of disk exhaustion</li>\n<li>Not suitable for multi-user environments</li>\n</ul>\n<hr>\n<h2>Option 3: Single XFS + Project Quota (<strong>Chosen</strong>)</h2>\n<pre><code class=\"hljs language-bash\">/data\n/data/persistent\n/data/scratch\n</code></pre>\n<p>Use:</p>\n<ul>\n<li>XFS as the underlying filesystem</li>\n<li>Project quota (pquota) to enforce limits</li>\n</ul>\n<hr>\n<h1>Decision Trade-offs</h1>\n<p>This decision is fundamentally a trade-off between:</p>\n<h3>1. Isolation vs Flexibility</h3>\n<table>\n<thead>\n<tr>\n<th>Approach</th>\n<th>Isolation</th>\n<th>Flexibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Partition split</td>\n<td>Strong</td>\n<td>Low</td>\n</tr>\n<tr>\n<td>XFS + pquota</td>\n<td>Medium</td>\n<td>High</td>\n</tr>\n</tbody>\n</table>\n<p>We chose <strong>flexibility</strong> because:</p>\n<ul>\n<li>Early-stage workload is uncertain</li>\n<li>Data vs scratch usage ratio may evolve</li>\n<li>Repartitioning later is costly</li>\n</ul>\n<hr>\n<h3>2. Operational Safety vs Adaptability</h3>\n<p>Partition-based design:</p>\n<ul>\n<li>Safer by default</li>\n<li>Enforced by the OS</li>\n</ul>\n<p>Quota-based design:</p>\n<ul>\n<li>Requires correct configuration</li>\n<li>Depends on operational discipline</li>\n</ul>\n<p>We accepted this trade-off because:</p>\n<blockquote>\n<p>Our system is still evolving, and adaptability is more valuable than strict enforcement at this stage.</p>\n</blockquote>\n<hr>\n<h3>3. Observability vs Control</h3>\n<p>Partition split:</p>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">df</span> -h\n</code></pre>\n<p>→ Immediate visibility</p>\n<p>Quota-based:</p>\n<pre><code class=\"hljs language-arduino\">xfs_quota -x -c <span class=\"hljs-string\">\"report\"</span>\n</code></pre>\n<p>→ Requires tooling</p>\n<p>We accept slightly reduced observability in exchange for:</p>\n<ul>\n<li>Dynamic capacity management</li>\n<li>Avoiding premature optimization</li>\n</ul>\n<hr>\n<h3>4. Production Convention vs Practical Engineering</h3>\n<p>Typical production clusters use:</p>\n<pre><code class=\"hljs language-bash\">/data\n/scratch\n</code></pre>\n<p>Separate mount points.</p>\n<p>We intentionally diverge because:</p>\n<ul>\n<li>This is an early-stage system</li>\n<li>We prioritize iteration speed over convention</li>\n<li>We plan to evolve toward stricter isolation later if needed</li>\n</ul>\n<hr>\n<h1>Decision Rationale</h1>\n<p>We chose <strong>XFS + project quota</strong> based on the following reasoning:</p>\n<hr>\n<h2>1. Workload is large-file and throughput-heavy</h2>\n<p>The system handles:</p>\n<ul>\n<li>Models (GB–TB scale)</li>\n<li>Datasets</li>\n<li>Checkpoints</li>\n<li>Container layers</li>\n</ul>\n<p>XFS is well-suited for:</p>\n<ul>\n<li>Sequential I/O</li>\n<li>Large file handling</li>\n<li>High throughput operations</li>\n</ul>\n<hr>\n<h2>2. Need for dynamic capacity allocation</h2>\n<p>We do not yet know:</p>\n<ul>\n<li>Exact ratio of persistent vs temporary data</li>\n<li>Future workload patterns</li>\n</ul>\n<p>Using pquota allows:</p>\n<ul>\n<li>Adjusting limits without repartitioning</li>\n<li>Avoiding irreversible decisions</li>\n</ul>\n<hr>\n<h2>3. Scaling strategy does not depend on shrinking</h2>\n<p>We explicitly decided:</p>\n<ul>\n<li>\n<p>No reliance on shrinking volumes</p>\n</li>\n<li>\n<p>Scaling will be done by:</p>\n<ul>\n<li>Adding SSDs</li>\n<li>Integrating NAS</li>\n</ul>\n</li>\n</ul>\n<p>This removes a key disadvantage of XFS.</p>\n<hr>\n<h2>4. Early-stage system prioritizes iteration</h2>\n<p>At this stage:</p>\n<ul>\n<li>Requirements are evolving</li>\n<li>Usage patterns are not fully stable</li>\n</ul>\n<p>We optimize for:</p>\n<blockquote>\n<p>Speed of iteration over rigid correctness</p>\n</blockquote>\n<hr>\n<h2>5. Acceptable level of risk</h2>\n<p>We acknowledge:</p>\n<ul>\n<li>Quota is not as strong as partition isolation</li>\n<li>Misconfiguration is possible</li>\n</ul>\n<p>But we mitigate this by:</p>\n<ul>\n<li>Clear directory structure</li>\n<li>Explicit usage guidelines</li>\n<li>Monitoring and documentation</li>\n</ul>\n<hr>\n<h1>Final Layout</h1>\n<pre><code class=\"hljs language-bash\">/            → ext4 (system)\n/data        → XFS\n  /persistent\n  /scratch\n</code></pre>\n<p>Quota is enforced via:</p>\n<ul>\n<li>XFS project quota</li>\n<li>Separate limits for persistent and scratch</li>\n</ul>\n<hr>\n<h1>Operational Guidelines</h1>\n<h2>Directory Usage</h2>\n<h3>Persistent Data</h3>\n<pre><code class=\"hljs language-bash\">/data/persistent\n</code></pre>\n<ul>\n<li>models</li>\n<li>datasets</li>\n<li>repositories</li>\n<li>shared artifacts</li>\n<li>Docker root</li>\n</ul>\n<hr>\n<h3>Temporary Data</h3>\n<pre><code class=\"hljs language-bash\">/data/scratch\n</code></pre>\n<ul>\n<li>checkpoints</li>\n<li>cache</li>\n<li>build artifacts</li>\n<li>intermediate outputs</li>\n</ul>\n<hr>\n<h2>Slurm Example</h2>\n<pre><code class=\"hljs language-bash\"><span class=\"hljs-built_in\">export</span> TMPDIR=/data/scratch/<span class=\"hljs-variable\">$USER</span>/<span class=\"hljs-variable\">$SLURM_JOB_ID</span>\n<span class=\"hljs-built_in\">mkdir</span> -p <span class=\"hljs-variable\">$TMPDIR</span>\n</code></pre>\n<hr>\n<h2>Docker</h2>\n<p>Docker root should be moved to:</p>\n<pre><code class=\"hljs language-bash\">/data/persistent/docker\n</code></pre>\n<hr>\n<h1>Future Evolution</h1>\n<p>This design is not final.</p>\n<p>Possible future changes:</p>\n<ul>\n<li>Split <code>/data</code> and <code>/scratch</code> into separate volumes</li>\n<li>Introduce network storage (NAS / distributed FS)</li>\n<li>Move heavy workloads to dedicated compute nodes</li>\n</ul>\n<hr>\n<h1>Lessons Learned</h1>\n<ol>\n<li>Storage design is about trade-offs, not absolutes</li>\n<li>Filesystem choice must match workload characteristics</li>\n<li>Early-stage systems benefit from flexibility</li>\n<li>Isolation can be introduced later, but rigidity is hard to undo</li>\n<li>Production design evolves — it is not fixed from day one</li>\n</ol>\n<hr>\n<h1>Conclusion</h1>\n<p>We chose <strong>XFS + project quota</strong> because it provides:</p>\n<ul>\n<li>High performance for data-heavy workloads</li>\n<li>Flexibility during early-stage development</li>\n<li>A balance between structure and adaptability</li>\n</ul>\n<blockquote>\n<p>Instead of optimizing for a perfect final state, we optimized for a system that can evolve.</p>\n</blockquote>"
  }
};
